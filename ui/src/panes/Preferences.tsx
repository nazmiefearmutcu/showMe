// Preferences was split into ./preferences_pane/* for maintainability.
// (folder is `preferences_pane` instead of `preferences` to avoid macOS case
//  insensitive conflict with Preferences.tsx)
// See ui/src/panes/preferences_pane/index.tsx for the implementation.
export { Preferences } from "./preferences_pane";
export { default } from "./preferences_pane";
