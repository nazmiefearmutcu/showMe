// ANR was split into ./anr_pane/* for maintainability.
// (folder is `anr_pane` instead of `anr` to avoid macOS case-insensitive
//  conflict with ANR.tsx)
// See ui/src/functions/anr_pane/index.tsx for the implementation.
export { ANRPane } from "./anr_pane";
