# Icons

Round 12 placeholder. Replace with branded artwork before first signed build.

Required artefacts (generated by `cargo tauri icon ../assets/showme-master.png`):

- `32x32.png`
- `128x128.png`
- `128x128@2x.png`
- `icon.icns` (macOS)
- `icon.ico` (windows)

For dev runs Tauri falls back to the built-in icon if these are missing.
