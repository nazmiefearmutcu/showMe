# ShowMe Manual Function Audit - 2026-05-03

Audit method: each function is opened in the desktop ShowMe UI, its `i` help panel is read, `Run` is pressed, and the visible output is assessed for completeness, controls, chart/table realism, user explanation, warnings, paper-position handling, and formula/symbol usability.

Runtime under test: `/Applications/showMe.app`, sidecar `127.0.0.1:64864`, status `HEALTHY`.

## Findings Log

| # | Function | Category | i / task text checked | Run result checked | Bugs / concerns |
|---:|---|---|---|---|---|
| 1 | MLSIG - ML Signal Classifier | portfolio | Help says "Train a classifier on technical features -> predict next N-day direction" and "Scope: global"; visible pane says `PORTFOLIO` and `Scope local portfolio`. | Run returns `DEGRADED`, `strategy_sharpe 0.42`, `test_accuracy 0.56`, source `ML_SIGNAL_MODEL`, coverage `BTCUSDT / CRYPTO / local_model`. | Scope contradiction between help and pane. No easy symbol/search selector even though output is symbol-specific. No timeframe/N-day control in the easy panel. Degraded warning is generic and does not explain why local model is degraded. Feature importance is shown without plain-language meaning. |
| 2 | ACCT - Multi-Account Aggregation | portfolio | Help says per-account roll-up/cross-account exposure and "Scope: global"; pane says `PORTFOLIO` and `Scope local portfolio`. | Run returns `DEGRADED`, 1 visible account row: `main`, 51 positions, total market value about `591,100.579356`, unrealized PnL `0`, source `YFINANCE`. | Scope contradiction. Portfolio realism concern: all shown exposure is `CRYPTO` and unrealized PnL is exactly `0`, with no account/position drilldown or explanation of cost basis. Warning is generic despite status `ok`. No controls to close old paper positions or inspect/close underlying positions from this pane. |
| 3 | BLAK - Black-Litterman | portfolio | Help says posterior expected returns combining market prior with views, "Scope: global"; pane says `PORTFOLIO` and `Scope local portfolio`; easy `RANGE` segmented control exists. | Run returns `DEGRADED`; rows are only `BTCUSDT`, `ETHUSDT`, `SOLUSDT`; market weights roughly `BTCUSDT 0.984`, `ETHUSDT 0.0157`, `SOLUSDT 0.000108`; `n_views 0`; source `YFINANCE`. | Scope contradiction. Core Black-Litterman inputs are not easy to edit: views, tau, delta, universe, risk aversion are hidden behind raw JSON. Because `n_views 0`, the function effectively returns market-prior/posterior equality without explaining that to the user. No chart or allocation comparison visualization. Generic degraded warning. |
| 4 | BMTX - Backtest Matrix | portfolio | Help says run multiple strategies across a symbol universe; pane says local portfolio; only `Deep`/`Advanced` visible, no strategy/universe controls. | Run returns `DEGRADED`; table has symbols/strategy/sharpe/return/drawdown/trades/final equity; chart label is `rows · total return`, x-axis is row index `1..9`, not symbol/strategy/time. | Missing easy controls for universe, strategy list, timeframe beyond raw JSON. Chart is generic row-index line, not a meaningful backtest or price/equity chart. Table metrics are not explained for an end user. Generic degraded warning without provider reason. |
| 5 | BTFW - Walk-Forward Backtest | portfolio | Help says registered strategy on historical OHLCV with equity curve/Sharpe/drawdown; easy `RANGE` exists; no visible symbol/strategy selector. | Run returns `DEGRADED`; metrics include total return about `-0.000498`, Sharpe about `-0.9849`, max drawdown `-0.000501`, trades `1`, samples `259`; chart `equity curve · equity`. | Strategy and target symbol cannot be chosen through an easy panel. Equity curve lacks clear date/time x-axis labels, making timeframe interpretation weak. Warning is generic. Metrics are shown without user-facing explanation of walk-forward windows or assumptions. |
| 6 | BTUNE - Backtest Auto-Tuner | portfolio | Help says hyperparameter sweep ranked by Sharpe/return/Calmar; easy `RANGE` exists; no visible grid/strategy/symbol controls. | Run returns `DEGRADED`; metrics include Sharpe about `1.136`, total return about `0.879`, max drawdown about `-1.0167`, Calmar about `0.535`, trades `15`; chart `rows · total return`. | Hyperparameter grid is not editable in the easy UI; user must use raw JSON. Chart is row-index total return, not a proper parameter surface/heatmap. Max drawdown below `-1` is suspicious and unexplained. Generic degraded warning. |
| 7 | CORR - Correlation Matrix | portfolio | Help says Pearson + Spearman + downside correlation for a symbol set; pane says local portfolio. | Run returns `DEGRADED`; visible rows only list `BTCUSDT`, `ETHUSDT`, `SOLUSDT` and `annualized_vol` values; source `YFINANCE`. | The function name promises a correlation matrix but no matrix/heatmap/correlation coefficients are visible. Symbol set cannot be chosen in an easy selector. Output mostly shows volatility, so the main task is not fulfilled. |
| 8 | GREEKS - Portfolio Greeks | portfolio | Help says sum delta/gamma/vega/theta/rho across an option book; no formula or model assumptions shown. | Run returns `DEGRADED`; single row `BTCUSDT call strike 105 spot 100 vol 0.35`, quantity and contract size are blank; totals delta/gamma/vega/theta/rho shown; source `GREEKS`. | Option book appears synthetic/incomplete: `T`, quantity, and contract size are missing. No easy panel for spot/strike/expiry/vol/book selection. No explanation of pricing model/equations behind Greeks. |
| 9 | LOTS - Tax Lots | portfolio | Help says open/list/sell tax lots with FIFO/LIFO/HIFO/specific-id. | Run returns `DEGRADED` and `empty: local portfolio has no tax lots`; next action says "Add a lot with action=open or import portfolio tax lots". | The UI gives no easy `open/list/sell`, method, quantity, price, or lot-id controls despite the function being action-oriented. User cannot follow the suggested next action without raw JSON. |
| 10 | MARS - Multi-Asset Risk | portfolio | Help is generic ("Run the function..."), not a real task description; pane says local portfolio. | Run returns `DEGRADED`; fields include `alpha_daily 0`, equal factor loadings `MKT/SMB/HML = 0.333333`, `r_squared 1`, vol/VAR/ETL; source `MULTI_ASSET_RISK_MODEL`. | Help text is insufficient. Equal factor weights and `r_squared 1` look placeholder-like unless justified. No factor model equation, benchmark/factor selection, confidence level, or horizon control. |
| 11 | MGN - Cross-Account Margin | portfolio | Help says margin requirements and buying power per account. | Run returns `DEGRADED`; row shows `account paper`, `crypto_futures`, cash `10,000`, market value `19,625`, equity `29,625`, buying power `276,625`, `1 positions`; source `MARGIN_ENGINE`. | Old paper position is still visible and there is no close/liquidate/drilldown flow from the pane. Margin methodology is not explained. Leverage/buying-power assumptions are opaque. Generic degraded warning. |
| 12 | PCAS - PCA Factor Stress | portfolio | Help says apply k-sigma shock along principal components. | Run returns `DEGRADED`; portfolio return `-5.328829%`, PnL `-31,498.738532`, `pc_index 0`, `k_sigma 3`, chart `rows · value`; source `PORTFOLIO_STATE_MODEL`. | Core parameters `pc_index` and `k_sigma` are not easy controls. Chart is generic row-index/value, not a PCA/factor/stress visualization. No eigenvector/factor explanation for end users. |
| 13 | PFA - Performance Attribution (Brinson) | portfolio | Help says Brinson-Hood-Beebower allocation/selection/interaction by sector. | Run returns `DEGRADED`; source count visible as `0`; chart `rows · port weight`; table has sectors and effects. | No data source is displayed despite numeric results. Benchmark/sector mapping cannot be selected. Chart shows portfolio weight by row index instead of attribution contribution, so it is not an appropriate visual for the task. |
| 14 | PORT - Portfolio | portfolio | Help is generic: "51 position(s) function..." and does not describe portfolio accounting, cost basis, or close-position workflow. | Refresh returns `OK`; market value about `$592k`, cost basis `$591,101`, unrealized P&L about `+$1.1k`, cash blank, 51 crypto rows, footer `yfinance, legacy ws 51/51 live`. | Top unrealized P&L is positive while visible per-position unrealized P&L rows are `$0`, so the totals do not reconcile. By-asset-class value does not visibly match the market-value card. No close/sell/edit control for old positions. No history chart or cost-basis explanation. |
| 15 | PORT_OPT - Portfolio Optimizer | portfolio | Help says Markowitz min-vol/max-Sharpe/risk-parity/efficient frontier; scope global, but pane scope says local portfolio. | Run returns `DEGRADED`; efficient frontier chart shows an abrupt step over row index `1..40`; rows only `BTCUSDT`, `ETHUSDT`, `SOLUSDT`; samples `365`, source `YFINANCE`. | Scope contradiction. Universe, objective, constraints, and risk-free rate are not easy controls. The frontier chart is visually present but looks like a generic line/area plot and does not expose risk on one axis and return on the other. |
| 16 | PORT_WHATIF - Portfolio What-If | portfolio | Help is generic and does not specify what scenario can be changed. | Run returns `DEGRADED`; chart `positions · quantity`; table mirrors current positions with quantity, avg cost, last, market value, unrealized PnL. | This is not a what-if interface: no scenario editor, add/remove/shock control, before-after comparison, or price-move graph. It behaves like a portfolio-position table under a what-if name. |
| 17 | PSC - Position Sizing Calculator | portfolio | Help says risk-based sizing with R-multiples and Kelly fraction. | Run returns `DEGRADED`; hard-coded looking values: account `10,000`, risk_pct `0.01`, entry `100`, stop `95`, target `115`, shares `20`, Kelly shares `40`; sources `NONE`. | Entry/stop/target/account/risk side cannot be changed through easy controls. No symbol selector or current price integration. Results are a raw key-value table, not an order-sizing workflow. |
| 18 | PVAR - Position-level VaR / MCR | portfolio | Help says per-symbol marginal contribution to portfolio risk and parametric VaR decomposition. | Run returns `DEGRADED`; portfolio annualized vol `0.087402`; chart `rows · weight pct`; table headers include marginal contribution and component risk. | Confidence level, horizon, covariance model, and symbol universe are not easy controls. Chart plots weight by row instead of risk contribution/VaR, so it does not visualize the promised decomposition. |
| 19 | REBA - Portfolio Rebalancer | portfolio | Help says compute orders to bring current portfolio to target weights. | Run returns `DEGRADED`; two sample orders: buy `BTCUSDT` 40 at 100 and `ETHUSDT` 32 at 125, gross notional `8,000`, source `REBALANCE_MODEL`. | Target weights/current weights are not editable except raw JSON. Orders look synthetic and are not tied to the 51-position portfolio visible in `PORT`. No trade preview/confirm/cancel workflow, and the chart is `rows · price` rather than drift/order notional. |
| 20 | RPAR - Risk Parity (ERC) | portfolio | Help says equal-risk-contribution weights for a given universe; range selector exists. | Run returns `DEGRADED`; weights for `BTCUSDT/ETHUSDT/SOLUSDT`, portfolio vol `0.463361`, risk contributions around one-third each, info method `inverse_vol`, source `YFINANCE`. | Function name says ERC but info says `inverse_vol`, which is not the same optimization unless explained. Universe/model constraints are not easy controls. No allocation/risk-contribution chart. |
| 21 | STRS - Portfolio Stress Test | portfolio | Help says apply historical and custom shock scenarios to portfolio. | Run returns `DEGRADED`; table lists scenario names/horizons such as GFC 2008, COVID 2020, Rate Shock +300bp, Crypto Winter; sources `NONE`; chart `rows · horizon days`. | The output lists scenario definitions but does not apply them to the actual portfolio P&L. No scenario selector or custom shock controls are available. The chart visualizes horizon length, not loss/impact. |
| 22 | TLH - Tax-Loss Harvesting | portfolio | Help says suggest loss lots, tax savings, and wash-sale-safe swaps. | Run returns `DEGRADED`; single `BTCUSDT` row: quantity `1`, avg cost `100`, current price `92`, unrealized PnL `-8`, estimated savings `1.2`, replacement ETF `-`; source `TAX_LOSS_MODEL`. | Appears synthetic and disconnected from the visible 51-position portfolio. Tax bracket, lot source, jurisdiction, wash-sale rule, and replacement choice are not easy controls. No sell/skip workflow. |
| 23 | TRA - Total Return Analysis | portfolio | Help is generic and does not define the asset/portfolio being analyzed. | Run returns `DEGRADED`; price_return_total and TWR both `1.191162`, CAGR `0.169863`, observations `1,255`, first date `2021-05-04`, last date `2026-05-01`, source `YFINANCE`. | No symbol/portfolio selector or cash-flow controls. Total return is shown without a price/return chart, dividend details, or explanation of TWR vs IRR. Scope global vs local portfolio contradiction remains. |
| 24 | WATCH - Live Watchlist | portfolio | Help says add AAPL/BTCUSDT, refresh live quotes, click symbols, remove rows, failures shown as `err`. | Refresh shows 6 symbols; `AAPL/MSFT/GOOGL/BTCUSDT/ETHUSDT` live via polling, `PENGUUSDT` shows `err`; add field is a plain text input. | Add-symbol field has no searchable symbol picker or suggestions. One row remains `err` while still marked `LIVE`, which is confusing. Removing rows uses an `x` button but there is no visible undo/confirmation, and there is no price mini-chart per symbol. |
| 25 | AIM - Order Management | trade | Help is generic and only says run/inspect/rerun; row count selector exists. | Run returns `DEGRADED`; table says `no_open_orders`, broker `paper`, status `ok`, sources `NONE`. | This does not function as order management for a user: no create/cancel/replace/close controls, no order detail drilldown, and no explanation of paper broker state. |
| 26 | BBGT - Multi-Asset Trade Ticket | trade | Help is generic and does not describe ticket fields. | Run returns `DEGRADED`; preview for paper broker, `AAPL`, asset class `EQUITY`, side `BUY`, quantity `0`, source `PAPER_TICKET`. | Trade ticket fields are not editable through easy controls. Quantity `0` makes the preview non-actionable. There is no symbol search, limit/market order type, time-in-force, risk preview, or submit/cancel workflow. |
| 27 | EMSX - Execution Management | trade | Help is generic and does not explain EMSX workflow. | Run returns `DEGRADED`; same paper preview shape as BBGT: `AAPL`, `BUY`, quantity `0`, source `PAPER_TICKET`. | Execution management is represented as a static paper ticket, not an execution blotter. No order route, child order, cancel, or fill-progress controls are visible. |
| 28 | EXEC - Execution Monitor | trade | Help promises live VWAP/TWAP slice-by-slice fill quality and pace tracking. | Run returns `DEGRADED`; one row `smoke-1`, `AAPL`, side `BUY`, target_qty `500`, arrival price `150`, algo `TWAP`, horizon `600`, status `live`, metrics say filled_qty `500` and residual `0`. | Status says live even though filled quantity equals target and residual is zero. Timestamp is a raw epoch-like number. No slice-by-slice chart, VWAP/TWAP benchmark, slippage, pace graph, or close/cancel controls are visible. |
| 29 | FXGO - FX Trading | trade | Help is generic and does not describe FX ticket requirements. | Run returns `DEGRADED`; paper preview for `EURUSD`, asset class `FX`, side `BUY`, quantity `0`, source `PAPER_TICKET`. | No currency-pair picker, tenor/settlement date, notional, side, order type, or quote panel. Quantity `0` means the default ticket is not useful. |
| 30 | TCA - Trade Cost Analysis | trade | Help says implementation shortfall, slippage, and opportunity cost across fills. | Run returns `DEGRADED`; summary `0 fields`, `n 0`, source `ORDER_HISTORY`. | The function has no fills but still reports generic degraded/ok. It does not explain that no order history exists, and it shows no TCA metrics, chart, or import/filter controls. |
| 31 | TSOX - Treasury Order Entry | trade | Help is generic and does not explain Treasury-specific order fields. | Run returns `DEGRADED`; paper preview for symbol `US10Y`, asset class incorrectly shown as `EQUITY`, side `BUY`, quantity `0`, source `PAPER_TICKET`. | Treasury order entry misclassifies `US10Y` as equity. No CUSIP/tenor/yield/price/notional/settlement controls. Quantity `0` and lack of ticket workflow make it non-actionable. |
| 32 | BQL - ShowMe Query Language | api | Help is generic; it does not expose query syntax except through output hints. | First load showed `NEEDS DATA`; pressing Run later returned `OK`, 90 rows for `BTC-USD`, close/volume table and a price chart from Feb 03 to May 03, source `YFINANCE`. | No easy query editor, symbol picker, period selector, or validation panel despite this being a query language. The status can flip from provider_unavailable to OK without a clear explanation. Chart exists but is generic `rows · close`. |
| 33 | BQUANT - BQuant Notebook | api | Help is generic and does not describe notebook launch or environment. | Run returns `DEGRADED`; values `showme.data`, `showme.functions`, `showme.portfolio`, `jupyter_url /notebook`, sources `NONE`. | No open-notebook button, no kernel/runtime status, no notebook list, and no explanation of how `/notebook` is reached. Static capability list is not enough for a notebook function. |
| 34 | DAPI - ShowMe Data API | api | Help is generic and does not explain auth, examples, or endpoint behavior. | Run returns `DEGRADED`; lists endpoints such as `/api/v1/quote/{symbol}`, `/history`, `/fundamentals`, `/portfolio`, `/order`, `/calendar`, sources `NONE`. | Endpoint list is static and not interactive. No request builder, copy/test controls, schema, response example, auth status, or warning that `/order` can be state-changing. |
| 35 | FLDS - Field Lookup | api | Help is generic and does not say how to search fields. | Run returns `DEGRADED`; 18 field descriptions such as price, open, volume, market_cap, beta, revenue, net_income; sources `NONE`. | Field lookup has no field search/filter input, category grouping, provider compatibility, or example usage. Degraded warning is shown even though the output is a static dictionary. |
| 36 | ISIN - Symbol Cross-Reference | api | Help says resolve ISIN/CUSIP/SEDOL/Ticker to OpenFIGI record and cross IDs. | Run returns `DEGRADED`; input `BTCUSDT`, id_type `ID_SEDOL`, `0 matches`, source `OPENFIGI`. | Default input is unsuitable for an ISIN/SEDOL lookup and returns no match. No easy input field, id-type selector, or symbol suggestions. The UI reports status ok while the actual lookup found zero matches. |
| 37 | ALLQ - Dealer Quotes (TRACE proxy) | bond | Help is generic and does not define the bond/security input or quote source. | Run returns `DEGRADED`; rows include Composite A/B and TRACE proxy quotes, bid around `99.57-99.66`, ask around `99.84-99.93`, mid `99.75`, source `TRACE_PROXY_MODEL`, chart `rows · bid`. | No bond/CUSIP/maturity selector. TRACE proxy/model is not a real dealer quote feed as presented. Chart is row-index bid only, not bid/ask depth or spread. No quote timestamp, size, dealer, or model explanation. |
| 38 | CRPR - Credit Rating Profile | bond | Help is generic and does not explain rating agencies, date, or issuer/security choice. | Run returns `DEGRADED`; ladder rows AAA through CCC; object issuer `US Treasury`, ratings `S&P AA+`, `Moody's Aa1`, `Fitch AA+`, implied bucket `high_grade`, source `MANUAL_OR_PUBLIC_DEFAULTS`. | Uses manual/public defaults instead of a live rating profile. No issuer/security selector, rating dates, outlooks, watchlist status, or rationale. Output is mostly a static rating ladder plus one issuer. |
| 39 | CRVF - Yield Curve | bond | Help is generic and does not expose curve country, date, tenor set, or source vintage. | Run returns `DEGRADED`; FRED values such as 1M `5.32`, 3M `5.28`, 2Y `4.62`, 10Y `4.45`, 30Y `4.67`; output is mainly table/key-value. | A yield-curve function should show a clear curve chart, but the visible result is not a proper maturity-axis curve. No country/curve/date controls. No vintage/date/source explanation beyond `FRED`. |
| 40 | DDIS - Debt Distribution by Maturity | bond | Help is generic and does not define issuer/debt schedule input. | Run returns `DEGRADED`; maturity buckets `0-1Y`, `1-3Y`, `3-5Y`, `5Y+`; total debt `26.7`; source `USER_INPUT_OR_DEBT_SCHEDULE_MODEL`; chart `rows · amount`. | No issuer/security input. Source indicates user/model data, not a live filing/debt schedule. Chart is row-index amount rather than a maturity-bucket bar with clear units. Currency/unit and methodology are not explained. |
| 41 | DEBT - Sovereign Debt Exposure | bond | Help is generic and does not define whether it is portfolio exposure or macro snapshot. | Run returns `DEGRADED`; rows for US, JP, DE, TR with debt-to-GDP and local-currency share; source `PUBLIC_MACRO_BASELINE`; chart `rows · debt to gdp`. | This is not portfolio exposure; it is a static macro baseline. No country/portfolio selector, holding weight, date, or source vintage. Chart is not geographic or portfolio-weighted exposure. |
| 42 | GC3D - Yield Curve 3D (live FRED) | bond | Help is generic; range buttons exist but no tenor/date/country controls are exposed. | Run returns `DEGRADED`; FRED source, 30 rows of date/tenor/yield, sample yields around `4.38-5.31`; chart `rows · yield`. | Function name promises a 3D yield-curve view, but UI shows a 2D line/area chart by row. No surface/heatmap/tenor-by-date visualization. Range can change horizon, but users cannot choose country, tenors, or date range cleanly. |
| 43 | SRSK - Sovereign Risk | bond | Help is generic and does not disclose the sovereign-risk formula or country input. | Run returns `DEGRADED`; country `TR`, proxy_spread_pct `3.25`, pd_1y_proxy `0.054167`, recovery `0.4`, source `FRED`. | Country is not selectable through an easy control. The probability-of-default proxy and recovery assumption are not explained. There is no country comparison chart, source date, or warning that this is a proxy rather than an observed market measure. |
| 44 | TAUC - Treasury Auction Calendar | bond | Help says upcoming/recent Treasury auctions; ROW and RANGE controls are visible. | Run returns `DEGRADED`; `treasurydirect: TimeoutError`, fallback source `TREASURY_AUCTION_FALLBACK`; 5 synthetic-looking rows with dates like `template+3d`; chart `rows · offering amount`. | Live provider failed and the fallback rows are template-relative, not actual calendar dates. Chart is generic row-index offering amount, not a calendar. No security type/term filter, auction status, announcement date, settlement date, or source timestamp. |
| 45 | WB - World Bonds | bond | Help is generic and does not define country set, tenor, or rate source. | Run returns `DEGRADED`; country values US `4.45`, DE `2.58`, JP `0.92`, GB `4.18`, FR `3.02`, IT `3.86`, ES `3.24`, AU `4.12`, source `FRED`. | Values are unlabeled in the table, so the user must infer these are yields and likely 10Y. No tenor/country/date controls and no chart/map. Generic degraded warning gives no useful action. |
| 46 | YAS - Yield & Spread Analytics | bond | Help is generic and says scope global; no bond or benchmark input is exposed. | Run returns `NEEDS DATA`; compatibility guard says `YAS does not support EQUITY` for requested `US10Y` even though this is a bond function; source `SHOWME_COMPATIBILITY_GUARD`. | Core function is blocked by wrong asset-class inference (`US10Y` treated as equity). User has no easy symbol/CUSIP/benchmark selector to fix it. No yield/spread calculation, duration, OAS, or equation is shown. |
| 47 | CHGS - Chart Studies | chart | Help says symbol field selects the market target; no study selection is explained. | Run returns `DEGRADED` for `BZ=F`; cards show last price `108.169998`, RSI14 `54.657604`, ATR14 `7.887773`, ADX14 `25.134828`; chart label is `rows · 2026-02-05...`. | Study parameters are fixed; users cannot choose indicators, periods, overlays, or timeframe beyond raw symbol/pills. The chart is not a proper price chart with studies overlaid; it plots a generic row/date series and does not label what value is being graphed. |
| 48 | GP - Price Graph | chart | Help says change visible controls, enter a symbol, then run/refresh; tabs for 1M/3M/6M/1Y/5Y exist. | Using `GO` for `BZ=F` returns `OK`; 1Y `251 candles`; TradingView candlestick/volume chart is visible with source `yfinance`. | This is one of the few screens with a real chart. Remaining issues: no normal Run button, symbol entry is a plain text field plus recent pills rather than searchable suggestions, and the help text is generic rather than explaining source, interval, and chart controls. |
| 49 | HP - Historical Price | chart | Help says HP downloads OHLCV rows for one ticker; range tabs and CSV button exist. | Using `GO` for `BZ=F` returns `OK`; 3M `63 bars`, high `118.35`, low `66.30`, total `63.15%`; table shows date/open/high/low/close/change/volume and CSV becomes enabled. | Data table is useful, but there is no price chart even though this is under chart functions. Symbol input is plain text plus recent pills, with no searchable full-symbol picker. Footer elapsed remains blank while data is shown. |
| 50 | TECH - Technical Indicators | chart | Help text is copied/generic from the price chart pane and does not describe any technical indicator calculations. | `BZ=F` and `AAPL` both return `OK` but show `No price data`, `0 candles`, source `yfinance`; refresh keeps the same result. | Function is effectively broken: status says OK even when no candles or indicators are returned. It reuses GP labels (`Price`, and empty-state text says `GP needs a ticker`) instead of a technical-indicator UI. No RSI/MACD/moving-average controls or equations are shown. |
| 51 | MEET - Meeting Briefing | comm | Help says pre-meeting briefing from Notion + Granola + portfolio + news + DES. | Run returns `DEGRADED`; one local briefing item for `Satoshi Nakamoto`, source `GRANOLA`, fields only `topic` and `status`. | No meeting picker, date/calendar selector, participant list, Notion/Granola connection status, portfolio/news/DES sections, or source snippets. Output is a placeholder-like local briefing and does not meet the stated multi-source briefing task. |
| 52 | PEOP - People Search | comm | Help says search executives, analysts, and contacts from local directory. | Run returns `DEGRADED`; query `Satoshi Nakamoto`, status `ok`, source `PEOPLE_DIRECTORY`, with no actual people rows visible. | No visible search input for the person/company/role, no suggestions, no result cards, and no contact/profile details. The fixed query makes the function look non-interactive despite being a search feature. |
| 53 | BGAS - Natural Gas Spot | commodity | Help is generic and does not describe spot/futures source, unit, or contract. | Run returns `DEGRADED`; symbol `NG=F`, fields `last`, `prev`, `high`, `low` all `-`, status `ok`, source `YFINANCE_FUTURES`. | Reports OK even though all price values are missing. No spot/futures distinction, contract month, unit, chart, region, or symbol selector. |
| 54 | BOIL - Oil Spot | commodity | Help is generic and does not explain WTI/Brent contract choice or units. | Run returns `DEGRADED`; WTI `CL=F` and Brent `BZ=F` both show `last: -` and `prev: -`, source `YFINANCE_FUTURES`. | Function produces no usable spot prices but still says OK. No price chart, contract expiry, unit, source timestamp, or WTI/Brent selector. |
| 55 | CPF - Commodity Price Forecasts | commodity | Help is generic; no forecast provider/date/horizon controls are explained. | Run returns `DEGRADED`; source `WORLD_BANK_FORECAST_BASELINE`; rows for crude oil `82 USD/bbl`, natural gas `3.6 USD/MMBtu`, gold `2450 USD/oz`, copper `9700 USD/mt`, all year `2026`; chart `rows · year`. | Chart is meaningless because every row has the same year, so it draws a flat row-index chart instead of comparing forecast values or showing time series. Forecast baseline/vintage is not shown, and commodity/horizon/scenario controls are absent. |
| 56 | GLCO - Global Commodity Movers | commodity | Help says global commodities refresh every 60 seconds; sector tabs exist. | Initial load showed `0 contract(s)` and `No contracts`; manual refresh briefly returned 2 rows (`CL=F`, `NG=F`) but name/sector/change/contract/open-interest fields were blank, then later the pane again showed no rows. | Live commodity mover table is unstable and mostly empty. Even when rows appear, most explanatory columns are blank. No price-change chart, no source/provider warning, and no way to choose exchange/contract universe beyond coarse sector tabs. |
| 57 | NGAS - Natural Gas | commodity | Help is generic and does not define the contract, unit, or quote source. | Run returns `DEGRADED`; symbol `NG=F`, `last`, `prev`, `high`, `low` all `-`, status `ok`, source `YFINANCE_FUTURES`. | Same failure as BGAS: no usable price data while status remains OK. No chart, expiry/month selector, unit, or fallback explanation. |
| 58 | WETR - Weather Trends | commodity | Help is generic and does not define location, forecast period, model, or provider. | Run returns `DEGRADED`; chart `rows · day`; 7 synthetic-looking days with temp `18-20.4C`, precip `3-1.8mm`; details show `lat 41.01 lon -74`, `provider_error OPENWEATHERMAP_API_KEY not set`, source `SEASONAL_WEATHER_MODEL`. | API key is missing and the function falls back to model data while still saying status `ok`. Location is not editable through easy controls, the chart plots day number rather than temp/precip, and the user is not told how weather affects commodities. |
| 59 | GEX - Gamma Exposure | derivative | Help says per-strike dealer gamma exposure, flip, and walls; symbol field exists. | Run returns `DEGRADED` for `AAPL`; chart `rows · strike`; three strikes 90/100/110 with GEX values; spot fixed at `100`; source `YFINANCE_QUOTE` plus `GAMMA_EXPOSURE_MODEL`. | Looks model-generated rather than real option-chain dealer exposure: only 3 strikes, no expiry/contract filters, no OI/volume/contract size, no gamma formula, and no explanation of why call GEX is negative and put GEX positive. Chart plots strike by row but not a proper exposure bar/flip visualization. |
| 60 | HVT - Historical Volatility Trends | derivative | Help says symbol field selects the target but does not define realized-vol formula. | Run returns `DEGRADED` for `AAPL`; `rv_30d_annualized 0.239401`, `rv_60d 0.254084`, `rv_90d 0.242586`, `rv_252d 0.231612`, source `YFINANCE`. | No volatility trend chart despite the name. Lookback windows are fixed and not easy controls. Formula is not explained, and there is no comparison against implied volatility or price history. |
| 61 | IVOL - Implied Vol Surface | derivative | Help is generic and does not explain surface axes, expiries, strikes, or source limits. | Run returns `DEGRADED` for `AAPL`; chart labeled `calls grid · strike`, min strike `150`, max `400`; visible value rows are expiration dates `2026-05-04`, `2026-05-06`, `2026-05-08`; source `YFINANCE`. | Not a real implied-vol surface in the UI: no heatmap/3D surface, no IV values visible in the table area, and no expiry/strike/type controls. Chart is a single irregular line by strike, not volatility over strike and expiry. |
| 62 | OMON - Option Monitor | derivative | Help is generic and does not explain what an option monitor should show. | Run returns `DEGRADED` for `AAPL`; it lists 21 expiration dates as a single `VALUE` column, source `YFINANCE`. | Option monitor is effectively just an expiration-date list. No calls/puts chain, strikes, bid/ask, IV, volume, OI, greeks, filters, or contract drilldown. |
| 63 | OSA - Option Strategy Analysis | derivative | Help is generic and exposes only raw Advanced for params. | Run returns `DEGRADED`; two hard-coded call legs: `+1` 100 call and `-1` 110 call, expiry `0.25`, vol `0.25`, source `NONE`; chart `rows · qty`. | Strategy legs are not editable in a usable panel. No payoff/P&L curve, breakeven, max gain/loss, greeks, scenario controls, underlying price, or volatility/rate inputs. Chart of quantity is not a strategy analysis. |
| 64 | OVME - Option Valuation | derivative | Help is generic; output states model `bs` but no equation/help definition is shown. | Run returns `DEGRADED`; Black-Scholes-like values: price `3.977576`, delta `0.421466`, gamma `0.027942`, theta `-0.034715`, vega `0.195594`, rho `0.095423`; inputs spot `100`, strike `105`, T `0.25`, vol `0.28`, rate `0.045`, type `CALL`, source `NONE`. | Calculation is transparent enough to infer Black-Scholes, but the equation and units are not explained. Spot/strike/T/vol/rate/dividend/type are not easy controls, so the user cannot practically value a chosen contract without raw JSON. No sensitivity chart or payoff/Greek visualization. |
| 65 | ANR - Analyst Recommendations | equity | Help says it should show Strong Buy/Buy/Hold/Sell/Strong Sell distribution plus 12-month mean/median/min/max price target. | Run returns `DEGRADED`; recommendation counts are present (`StrongBuy 8`, `Buy 18`, `Hold 12`, `Sell 2`, `StrongSell 0`), but all price-target fields are `-`; source `ANALYST_CONSENSUS_MODEL`. | Half of the stated task is missing: no 12-month target values. Source is a model rather than a named analyst feed, with no broker/date/history. No distribution chart, no target range chart, and no searchable symbol picker. |
| 66 | APPL - Industry Taxonomy | equity | Help is generic and only says to run the function and select a symbol. | Run returns `DEGRADED` for `AAPL`; table shows sector `Technology`, industry `Consumer Electronics`, country `United States`, currency `USD`, exchange `NMS`, source `YFINANCE`. | Useful basic metadata exists, but the taxonomy is shallow: no GICS/NAICS/ICB hierarchy, provider date, or classification history. It shows degraded even with yfinance output, and it does not explain exchange codes such as `NMS`. |
| 67 | BETA - CAPM Beta | equity | Help shows formula `beta = cov(r_i, r_m) / var(r_m)` and mentions benchmark/multiple windows. | Run returns `DEGRADED` for `AAPL`; windows include 1Y beta `1.001961`, 2Y `1.189433`, 5Y `1.224655`, benchmark `^GSPC`, source `YFINANCE`; chart label `rows · beta`. | Benchmark and windows are fixed in the visible UI; only raw Advanced can change them. Formula is shown but return frequency, dividend handling, and risk-free treatment are not explained. Chart is a row-index line, not beta over time. |
| 68 | CACT - Corporate Actions | equity | Help is generic and does not define action types or dates. | Run returns `DEGRADED`; output is 1000 SEC filing rows with fields such as `accessionNumber`, `filingDate`, `reportDate`, `act`, `form`, `items`; chart label `rows · act`. | This is SEC filings metadata, not corporate actions like dividends, splits, mergers, tender offers, or spin-offs. No action type/ex-date/payment-date filters. The `act` chart is meaningless for end users and the huge table is not explained. |
| 69 | DARK - Dark Pool Volume | equity | Help says FINRA ATS weekly off-exchange volume by venue. | Run returns `DEGRADED`; latest row visible is `weekStartDate 2023-11-06`, total share quantity `6,489,411,323`, total trade count `66,398,110`, `n_venues 0`, source `FINRA`. | Venue-focused task is not fulfilled: no venue breakdown and `n_venues=0`. Date is stale for a current market cockpit. No date range, symbol search, venue filter, venue share chart, or explanation of degraded status. |
| 70 | DCF - Discounted Cash Flow | equity | Help is generic and does not explain the DCF equation or inputs. | Run returns `DEGRADED`; model values include `wacc 0.09`, high growth `0.1`, terminal growth `0.025`, starting FCFE `1,000,000,000`, equity value `21.65B`, fair value per share `21.645295`, source `NONE`; chart `rows · year`. | It is a hard-coded model, not a company-specific DCF. No source for FCFE/shares/WACC/growth, no scenario controls except raw Advanced, no equation in help, and the chart plots forecast year rather than a valuation bridge. |
| 71 | DCFS - DCF Sensitivity | equity | Help promises a WACC x terminal-growth grid plus +/-20% input tornado. | Run returns `DEGRADED`; visible output only shows values `0.07`, `0.09`, `0.11` and `base_fair_value 13.282053`, source `NONE`; chart `rows · value`. | The promised sensitivity grid and tornado are missing. No editable WACC/terminal growth ranges in the main UI, no heatmap, no tornado chart, and no source/company-specific input basis. |
| 72 | DDM - Dividend Discount Model | equity | Help is generic and does not explain the Gordon-growth formula or assumptions. | Run returns `DEGRADED`; `dividend_ttm 1`, `next_dividend 1.03`, `growth 0.03`, `required_return 0.08`, `fair_value_per_share 20.6`, source `NONE`. | Hard-coded assumptions are used with no vendor/source. Required return and growth are not easy controls, the formula is not shown, and there is no dividend history or sensitivity chart. |
| 73 | DES - Description | equity | Help says it is a description pane with live/degraded state and source details. | GO on `AAPL` returns `OK`; sector `Technology`, industry `Consumer Electronics`, country `United States`, exchange `NMS`, currency `USD`, website `apple.com`, source `yfinance`, but business summary, employees, market cap, and IPO are blank. | Basic profile fields work, but the main business summary is empty. Key profile values are blank despite a yfinance source, and symbol entry is a plain field plus recent pills rather than searchable suggestions. |
| 74 | DPF - Dark Pool / ATS Volume | equity | Help says FINRA off-exchange ATS volume plus dark-pool percent of total; range controls are visible. | Run returns `DEGRADED`; range `1Y`, latest `weekStartDate 2023-11-06`, `ats_share_volume 6,502,724,597`, `ats_trade_count 66,530,750`, `raw_rows 208`, source `FINRA`. | It does not show the stated dark-pool percent of total. Data is stale, no venue/entity split is shown, range controls do not solve date staleness, and there is no volume/share chart. |
| 75 | DVD - Dividends & Splits | equity | Help is generic and does not describe ex-date/pay-date/split fields. | Run returns `DEGRADED`; row shows `date -`, `amount 0`, `status no_recent_dividend_feed`, source `YFINANCE`. | AAPL has dividend history, but the function reports no recent feed and still says status ok. Splits are not shown, no date range exists, and there is no dividend/split chart. |
| 76 | EE - Earnings & Estimates | equity | Help says historical earnings actual vs consensus, surprise %, and next estimate date. | Run returns `DEGRADED`; after rerun it shows 25 rows with EPS estimates, but visible `reported EPS` and `surprise(%)` fields are `-`; source state is degraded. | Estimates are present but actual/surprise coverage is missing in the visible result. No next earnings date is surfaced, no period filter, and chart is `rows · EPS Estimate` rather than actual vs estimate/surprise over time. |
| 77 | EREV - Earnings Revisions | equity | Help says analyst recommendation buckets month-over-month plus revision velocity. | Run returns `DEGRADED`; rows for `2026-02` to `2026-04`, scores `28/31/32`, current score `32`, `velocity_avg -0.015789`, source `FINNHUB`; chart `rows · score`. | Output is useful but still degraded without explanation. Score/velocity methodology is not explained, period selection is not exposed, and the chart is a simple score line rather than bucket-change or revision-flow visualization. |
| 78 | ESG - ESG Scores | equity | Help is generic and does not describe vendor, score scale, or controversy scale. | Run returns `DEGRADED`; all fields (`totalEsg`, environmental, social, governance, controversy) are `-`, status `no_vendor_score_available`, source `YFINANCE`. | Function effectively has no usable ESG data for AAPL. It lacks fallback provider explanation, score scale, methodology, peer comparison, chart, and a clear action for missing vendor data. |
| 79 | FA - Financial Analysis | equity | Help says income, balance, cash flow, and ratios; tabs are visible. | GO/Refresh on `AAPL` returns `OK`; SEC EDGAR data populates income and balance tabs, and the ratios tab shows cards such as gross margin `0.4927`, net margin `0.266`, ROE `0.2778`, current ratio `1.0704`, source `sec_edgar`. | This is one of the stronger equity views, but ratio formulas are not explained, there is no chart/trend visualization, and the symbol picker is still plain. Cash/balance/income tabs work, but end users get no units/date-context explanation beyond table headers. |
| 80 | FORM4 - Insider Transactions | equity | Help says recent SEC Form 4 insider filings for a ticker. | Run returns `DEGRADED`; table shows filing metadata such as accession, filing/report date, form `4`, primary document link, size and XBRL flags; chart `rows · form`. | It lists Form 4 filing documents, not parsed insider transactions. No insider name, transaction code, shares, price, ownership change, or buy/sell classification is extracted; charting the constant form number is meaningless. |
| 81 | FTS - SEC Full-Text Search | equity | Help says free-text SEC EDGAR search by form type and date range. | Run returns `OK`/array output with 100 rows and a score chart; visible rows include Morgan Stanley and ETF filings despite symbol `AAPL`. | There are no visible controls for free text, form type, or date range except raw Advanced. Result relevance is questionable for AAPL, snippets are collapsed to `1 snippet`, and the score chart does not explain matches. |
| 82 | HDS - Holders | equity | Help says holder rows and a ROW count control. | Run returns `DEGRADED`; visible chart label says `insider transactions · Shares` and table columns include shares/value/text/insider/position/transaction/start date/ownership. | A holders function is showing insider-transaction-style data rather than institutional holders. No holder type, filing date, ownership percent, top-holder ranking, or 13F source detail is clear. |
| 83 | HFS - Holder Search | equity | Help says 13F reverse lookup for filers holding an issuer/CUSIP. | Run returns `OK`; single row `No local 13F match`, issuer `AAPL`, shares `0`, market value `0`, source `SEC_13F`. | AAPL should have many institutional holders, so the local 13F data path appears empty or disconnected. There is no CUSIP/entity search control, no date/quarter selector, and no explanation of what local coverage means. |
| 84 | LITM - Litigation Monitor | equity | Help says recent 8-K Item 1.03/1.04/3.03 filings for bankruptcy, mine safety, and security holder rights. | Run returns `DEGRADED`; row shows symbol `AAPL`, category `litigation`, status `local_litigation_model`, `all_8k_events 0`, source `LITIGATION_MONITOR_MODEL`. | It is a local model with zero events, not a real litigation monitor. It does not show actual SEC links, legal cases, dates, severity, docket/source names, or a filter for event type/date. |
| 85 | PIB - Public Information Book | equity | Help is generic and does not define what should be included in a public information book. | Run returns `OK`/array with 50 SEC filing rows and chart `rows · act`; fields are filing metadata (`accessionNumber`, dates, `act`, `form`, XBRL flags). | A public information book should summarize company profile, filings, financials, news, comps, and links; this is just a raw SEC filings table. No book sections, export, or narrative summary exists. |
| 86 | RV - Relative Valuation | equity | Help is generic and does not describe peer universe or multiples. | Run returns `OK`; one row for `AAPL` with market cap `4.109T`, PE `33.915257`, forward PE `29.292562`, PB `46.70557`, PS `9.101958`, EV/EBITDA `25.821`, ROE `1.41471`, sources `FINNHUB` and `YFINANCE`. | Relative valuation needs peers, but only the target symbol is returned. No peer selection, industry comparison, percentile/ranking, chart, or multiple definitions are shown. |
| 87 | SPLC - Supply Chain (approximate) | equity | Help is generic and does not define supply-chain approximation method. | Run returns `DEGRADED`; output is SEC filing metadata with `accessionNumber`, filing/report dates, `act`, `form`, `fileNumber`, XBRL fields; chart `rows · act`. | Supply-chain data is not present at all. It appears to reuse SEC filings output, with no suppliers/customers, exposure weights, geography, source documents, network graph, or approximation methodology. |
| 88 | WACC - Weighted Average Cost of Capital | equity | Help is generic and does not state the WACC/CAPM equations. | Run returns `DEGRADED`; `wacc 0.08898`, `re_capm 0.09`, `rf 0.04`, `beta 1`, `erp 0.05`, `rd 0.05`, `tax_rate 0.21`, equity weight `0.9798`, debt weight `0.0202`, source `YFINANCE`. | Inputs are mostly fixed assumptions and not editable in a normal panel. CAPM/WACC formulas and units are not explained, beta is hardcoded to `1`, and no sensitivity/chart is provided. |
| 89 | FRD - FX Forward Rates | fx | Help is generic and only says scope `global`; it does not explain covered-interest-parity math. | Run returns `DEGRADED`; `S 1.1`, `F 1.098682`, `r_base 0.045`, `r_quote 0.04`, `T 0.25`, source `NONE`. | Forward calculation is hard-coded and source-free. No currency pair selector, tenor selector, yield-curve inputs, equation, or units are exposed. |
| 90 | FXFC - FX Forecasts | fx | Help is generic and does not define forecast model or pair selection. | Run returns `DEGRADED`; pair `EURUSD`, spot `1.1`, forecasts `1M 1.1022`, `3M 1.1066`, `12M 1.1198`, source `FORWARD_POINTS_MODEL`; chart `rows · spot`. | Forecasts are model-derived and pair/horizon controls are not visible. The chart plots constant spot, not forecast path or uncertainty; model assumptions and confidence intervals are missing. |
| 91 | FXH - FX Hedge | fx | Help says forward-rate overlay calculator for foreign-currency exposure; range controls exist. | Run returns `DEGRADED`; default currency row is `USDT` vs home `USD`, notional `591,100.579356`, hedge ratio `1`, days `365`, total carry PnL `2,841.829708`; both home-strengthens and home-weakens PnL are the same; sources `YFINANCE`, `ECB`. | Exposure cannot be edited through normal controls. Defaulting to `USDT` is odd for an FX hedge, and identical strengthen/weaken PnL means scenario logic is not realistic. No payoff/hedge effectiveness chart is shown. |
| 92 | FXIP - FX Information Portal | fx | Help is generic and does not define included FX metrics. | Run returns `DEGRADED`; pair `EURUSD`, spot `1.0835`, daily change `0.18`, 1M forward `1.0848`, 3M forward `1.087617`, ATM 1M IV `8.45`, carry `0.0175`, source `EXCHANGERATE_HOST`. | Useful snapshot fields exist, but pair selection is hidden, IV source/method is not explained, and there is no chart, time stamp, bid/ask, or central-bank/rate context. |
| 93 | OVDV - FX Option Volatility Surface | fx | Help is generic and does not explain tenors, deltas, or volatility source. | Run returns `DEGRADED`; 25 rows with tenor/delta/vol such as `1W 10P 9`, `1W 25P 8.75`, chart `rows · vol`. | Not a real surface view: no heatmap/3D surface, no pair selector, no expiry/delta controls, and no source/methodology visible. The chart is a row-index line over vol. |
| 94 | WCRS - World Cross Rates | fx | Help says world currency cross rates with base tabs and 30-second refresh. | Refresh returns `OK` but `0 pair(s)` and empty state `No crosses` for base `USD`; tabs exist for USD/EUR/GBP/JPY/TRY. | Core output is empty while status is OK. No provider/source/error message, no table, no heatmap, and no explanation of why no crosses are available. |
| 95 | BTMM - Country Rate Environment | macro | Help is detailed: country buttons isolate one policy-rate series; region buttons switch G10/EM/Europe/Americas/APAC/MEA; latest rate is BIS CBPOL, last move compares previous different rate, and 3M bp compares roughly 90 calendar days earlier. | Initial load sat empty/disabled, then after about 19s returned `OK`: `10/49 rate rows · ALL · G10`, avg `2.4475%`, high `4.10%`, low `0.00%`, hikes `2`, cuts `8`, largest move `US -25bp`; rows show BIS CBPOL dates and region/country controls. | This is one of the better macro views, but first load is slow and looks empty while waiting. It has no rate-history chart, no yield-curve/context chart, no search over the 49 rows, and no stale-data warning beyond row dates. |
| 96 | COUN - Country Guide | macro | Help is generic: run, inspect source strip, use Advanced for raw params, Deep for broader providers. | Run returns `DEGRADED`; table shows series IDs `GDPC1`, `CPIAUCSL`, `UNRATE`, `DGS10` with `3 result`, `country US`, sources `SEED` and `FRED`. | This is not a country guide. It does not show a country narrative, key current values, units, latest dates, charts, source links, or a country selector except raw Advanced; `3 result` is opaque. |
| 97 | ECFC - Economic Forecasts | macro | Help is generic and does not explain the forecast variables, country, or providers. | Run returns `DEGRADED`; 53 rows/1 field, source `IMF`; visible output is year columns from `1980` onward with values like `-0.3`, `2.5`, `-1.8`; warnings list `imf.PCPIPCH`, `imf.LUR`, `imf.GGXCNL_NGDP`, `oecd`. | Metrics are hidden in source keys instead of user-readable labels, so the table is hard to interpret. No country/variable/horizon selector, no forecast-vs-actual chart, and warnings say provider keys without explaining what failed. |
| 98 | ECO - Economic Calendar | macro | Help says to change visible controls, enter symbol/parameters when shown, then run or refresh; it does not define fields. | `This week` refresh returns `OK` but `0 event(s)`. Switching to `This month` returns 2 US events: `2026-05-13 CPI` high and `2026-05-15 Retail sales` medium, with forecast/actual/surprise blank; source `tradingeconomics`. | Week/month tabs work, but there is no country/region/importance/date-range filter. Actual/forecast/surprise are blank, empty-week state still says OK without context, and the help is boilerplate. |
| 99 | ECST - Economic Statistics | macro | Help is generic and does not identify the statistic, country, unit, or series code. | Run returns `DEGRADED`; warning `fred: FRED_API_KEY not set`, sources `WORLDBANK` and `MACRO_SERIES_BASELINE`; chart/table show three values for `2026-01-01` to `2026-03-01` from `3.1` to `2.9`. | The main statistic is unnamed: users cannot tell whether this is CPI, GDP, unemployment, etc. FRED failure degrades the result, no series/country/date controls exist except Advanced, and the chart lacks units/source-period explanation. |
| 100 | GMM - Global Macro Movers | macro | Help is generic and only says global scope plus Advanced/Deep. | Run returns array with 2 rows: US CPI actual `3.1` vs forecast `3`, EU PMI actual `51.2` vs forecast `50.7`; source `TRADINGECONOMICS`; chart label `rows · actual`. | The chart is misleading because it connects CPI percent and PMI index values on one row-index line. No region/importance/date filter, no ranking methodology, no event timestamps, and only two sample-like movers are shown. |
| 101 | REGM - Market Regime | macro | Help says it classifies regime via trend, vol, drawdown, curve, optionally cluster history. | Run returns `DEGRADED`; symbol `SPY`, trend `SIDEWAYS`, `ma50_vs_200_pct -0.524047`, vol `NORMAL`, realized vol `11.405111`, drawdown `0`, curve `UNKNOWN`, regime `Range-bound`, sources `YFINANCE` and `FRED`. | It exposes components but not the threshold rules or equations. Symbol is fixed to SPY in the normal UI, curve is unknown while FRED is listed, and degraded status just says `ok` without a concrete failed source. No regime-history chart. |
| 102 | TRDH - Trading Hours | macro | Help says per-exchange trading-session status plus next open/close in UTC. | Run returns 10 exchange rows in 1-4ms, including NYSE/NASDAQ/LSE/FWB/TYO/HKEX/ASX with local hours, timezone, `is_open_now`, next open UTC and seconds until open; chart label `rows · seconds until open`. | Table is useful, but the chart is a row-index line across unrelated exchanges, not a session timeline. No exchange search/filter, holiday/source provenance is not shown, and truncated columns make timezone/open times harder to inspect. |
| 103 | WIRP - World Interest Rate Probability | macro | Help is generic and does not define the probability model, jurisdiction, or meetings. | Run returns `DEGRADED`; two meeting dates `2026-06-10` and `2026-07-29` with `cut_25bp` `0.18/0.28`, `hold` `0.72/0.62`, `hike_25bp 0.1`, source `FEDWATCH_PROBABILITY_MODEL`; chart only plots `cut_25bp`. | This is not world coverage; it is a small FedWatch-style model. No central-bank selector, no futures/implied-rate source, no equation/method, no stacked probability chart, and degraded status gives no actionable source warning. |
| 104 | ALRT - Alerts | misc | Help is boilerplate but visible controls include symbol, field, direction, threshold, note, refresh, add, disable, test, and delete. | I set `AAPL` / price above / `99999` and clicked Add; UI created `1 alarm(s)` with row `AAPL price ABOVE 99,999.0000`, fired count `0`, `test` and delete controls, storage `preset filesystem`, notification available. | Basic alert creation works, but symbol entry is a plain text field with no quote validation or search suggestions. No current price, polling cadence, trigger history, or condition preview is shown. The test alert remains in local state because deleting local data requires explicit cleanup confirmation. |
| 105 | BIO - Biometric Auth | misc | Help is generic and does not explain WebAuthn registration/verification steps. | Run returns `DEGRADED`; rows only show `register` and `verify`, `webauthn available`, source `LOCAL_AUTH`. | It does not expose actual register/verify buttons or a biometric prompt. No relying-party/user/device state, credential list, security explanation, or failure path is visible; status is degraded while the message says ok. |
| 106 | BMC - Market Concepts Education | misc | Help is generic and does not explain how to open lessons. | Run returns `DEGRADED`; values are category names only: Equities, Fixed Income, FX, Commodities, Macro, Alternatives with lesson counts; source `LOCAL_CURRICULUM`. | It is a static curriculum index, not an education experience. No lesson content, progress, quiz, search, examples, or click-through controls are visible, and degraded status gives no real warning. |
| 107 | CDE - Custom Data Fields | misc | Help is generic and does not define field syntax or supported operations. | Run returns `DEGRADED`; values are `list`, `add`, `remove`, `evaluate`, with `fields 0`, source `NONE`. | The function lists verbs but provides no UI to create, edit, remove, or evaluate fields. No formula editor, field schema, target symbol/universe, validation, or persistence view is shown. |
| 108 | DINE - Restaurants | misc | Help is generic and does not define location/search/rating source. | Run returns `DEGRADED`; two local rows: `Market Cafe` and `Terminal Grill`, both New York, ratings `4.4/4.2`, price `$$`, source `LOCAL_DIRECTORY`; chart `rows · rating`. | This is a local sample directory, not a restaurant finder. No location/search/map/open-now/cuisine controls, no real provider/source timestamp, and the rating chart is a two-point row-index line. |
| 109 | FLY - Flight Tracking | misc | Help is generic; task name implies live flight tracking. | Run returns `NEEDS DATA`; warning says `tracking_unavailable`, but still shows sample flights `UAL238 KJFK-KLAX 35000` and `THY12 LTBA-KJFK 37000`, source `OPENSKY_FLIGHT_BASELINE`; chart `rows · altitude ft`. | The function mixes an unavailable-state warning with plausible-looking baseline data, which is misleading. No flight/callsign/airport/date input panel, map, route track, live timestamp, or provider auth guidance beyond generic text. |
| 110 | GRAB - Screenshot Email | misc | Help is generic and does not specify capture target, recipient, or send mechanism. | Run returns `DEGRADED`; `queued yes`, `target current`, source `LOCAL_CAPTURE_QUEUE`; no screenshot preview, file path, recipient, or email status appears. | The “Email” part is not implemented in the UI result. It queues something locally but does not show the captured image, where it was saved, who receives it, or whether sending is configured. |
| 111 | LANG - Language Switch | misc | Help is generic and does not list available languages or whether it changes the app UI. | Run returns `DEGRADED`; `lang tr`, `status ok`, source `NONE`; the surrounding app UI stays in English. | It reports Turkish but does not visibly switch interface language. No language picker, apply/revert controls, persistence status, translation coverage, or restart requirement is shown. |
| 112 | ONCH - On-Chain Metrics | misc | Help says crypto on-chain metrics: fees, hash rate, active addresses, gas, mempool. | Run returns `DEGRADED`; Glassnode price/MVRV/active-address/tx-count warnings appear, then a chart/table of Bitcoin block heights around `947,727`; source is effectively mempool/block data. | Most promised metrics are missing due auth/tier errors. The visible chart is block height over row order, not fees/hash rate/addresses/gas/mempool, and there is no asset/network/metric/date selector in the normal UI. |
| 113 | POLY - Polymarket | misc | Help says prediction-market odds from Polymarket public CLOB markets. | Run returns `OK`; six rows with questions, slugs, volume/liquidity, outcome prices and outcomes; examples include May 3 esports/soccer markets and a stale `2026-03-06` market; chart `rows · volume`. | Data loads from Polymarket, but filtering is weak: no search/category/status/end-date controls, some ended/past markets appear, prices are raw JSON strings, and charting volume by row order is not enough for market analysis. |
| 114 | SAT - Satellite Imagery | misc | Help promises Sentinel-2 true-color PNG plus NDVI stats for a bbox and date window. | Run returns `DEGRADED`; only `bbox -122.55,37.70,-122.30,37.85`, `date_from 2026-04-26`, `date_to 2026-05-03`, source `SENTINELHUB`; no image or NDVI values render. | Core promise is missing: no satellite image, NDVI stats, cloud cover, map, bbox/date controls, or preview/download. It shows degraded but message says ok. |
| 115 | WHAL - Whale Alerts | misc | Help says large on-chain transfers and balance moves from Glassnode, Etherscan, and Mempool. | Run returns `DEGRADED`; table shows Bitcoin block-header fields such as hash/id, height, version, timestamp, bits, nonce, difficulty, tx_count, size, weight; chart `rows · height`. | This is not a whale-alert feed. It lacks large transfer amount, token, source/destination wallet, exchange labels, USD value, threshold controls, and alert severity; the current output is generic block data. |
| 116 | AV - Audio/Video Archive | news | Help is generic: run, inspect source strip, ROW controls result count, Advanced for JSON overrides, Deep for broader provider flag. | Run returns `OK`; `ARRAY 2`, `ROWS 4`, source `PODCAST_DIRECTORY`; rows include `Market Audio` and `Earnings Replay`, but link and published fields are `-`. | Static/sample-like output. No playable audio/video, no working links, no published dates, no feed/date/search controls, and ROW 50 still returns only two visible items. |
| 117 | BRIEF - Daily Brief | news | Help is the same generic global ROW/Advanced/Deep text. | Run returns `OK`; `STRING`, `0 ROWS`, source `WATCHLIST_CACHE`, empty state `No usable rows`. | Daily brief produces no brief. It reports OK despite empty content, does not explain watchlist/source state, and has no date/session/source controls. |
| 118 | CN - Company News | news | Help says this is a polling news function; visible controls include symbol field, recent chips, LAST count buttons, and Refresh headlines. | Run/GO on `AAPL` returns `OK`; 15/100 RSS/Nasdaq headlines load, with titles and relative times. | Data loads, but relevance is noisy: broad S&P/Nasdaq and Altria-style headlines appear in an AAPL feed. Symbol entry has no search suggestions beyond chips, and there is no dedup, sentiment, date range, or provider filter. |
| 119 | EVTS - Corporate Events | news | Help is generic global ROW/Advanced/Deep text. | Run returns `DEGRADED`; row says `event earnings`, `date -`, `symbol AAPL`, `status no_event_feed`, source `YFINANCE`. | Corporate-events feed is not working. Date is missing, only a placeholder earnings row appears, and there are no visible symbol/date/event-type controls except Advanced. |
| 120 | NALRT - Critical News Alerts | news | Help says it ranks live headlines by market impact and raises critical/high news alerts; visible symbol field, chips, and ROW controls exist. | Run on `BTCUSDT` returns `DEGRADED`; 130 items scanned, alert count 1, top alert `CRITICAL 86` from Decrypt but timestamp is `3 AY ONCE`; chart is `top importance score`. | A critical live alert is three months old, so freshness handling is broken. Degraded state gives no concrete failed feed, alert thresholds/methodology are not explained, and no delivery/push setting is visible. |
| 121 | NI - News by Topic | news | Help/pane title behaves like company news, while function name says topic news. | GO on `BTCUSDT` returns `OK`; 61/100 crypto headlines load from Coindesk/Cointelegraph/TheBlock. | Misnamed/miswired UX: there is no topic keyword/category control; it is a symbol-news feed. No topic search, dedup, sentiment, date range, or source filter. |
| 122 | NSE - News Search Engine | news | Help is generic global ROW/Advanced/Deep text. | Run returns `DEGRADED`; warning `rss: slice indices must be integers or None or have an __index__ method`; `ARRAY 0`, `ROWS 0`, sources `NONE`. | Broken function. It surfaces a raw Python exception, returns no search results, and does not expose a normal query/search panel. |
| 123 | READ - Personalized News (For You) | news | Help is generic global ROW/Advanced/Deep text. | Run returns `OK`; 12 items/17 rows, chart `rows · importance_score`, RSS headlines plus fallback rows such as `AAPL company news feed unavailable` and `MSFT company news feed unavailable`; sources `LOCAL_NEWS_CACHE RSS`. | RSS headlines load, but personalization is unclear and mixed with unavailable-feed rows. No preference/watchlist controls, no source/date/topic tuning, and the importance-score chart is just a row-order line. |
| 124 | SOSC - Social Sentiment | news | Help is generic global ROW/Advanced/Deep text. | Run returns `DEGRADED`; one row `AAPL discussion placeholder`, score `0`; Stocktwits bullish/bearish/message counts all zero; sources `STOCKTWITS REDDIT SENTIMENT`. | Placeholder, not real sentiment. No symbol selector in the pane, no posts, no URLs, no sample size, no model explanation, and degraded warning only says `ok`. |
| 125 | TLDR - Daily TLDR | news | Help says LLM-summarised portfolio plus watchlist day in five bullets. | Run returns `DEGRADED`; calendar rows for US CPI/FOMC/Retail Sales plus markdown `ShowMe TLDR - 2026-05-03`; top movers up/down are blank; sources `YFINANCE CALENDAR_FEED_MODEL RSS`. | Summary is incomplete: top movers are empty, source headlines are mixed into one sentence, and it does not show the LLM/model provenance. No controls for portfolio/watchlist/date/session. |
| 126 | TOP - Top News | news | Help says 3/50 headlines, refresh 60s; LAST buttons and Refresh exist. | Refresh returns `OK`; only 3/50 RSS headlines show, including GlobeNewswire Exodus items and CNBC Bitcoin item. | Core feed is too sparse for a top-news screen. It lacks market/topic/source controls, shows raw HTML snippets, and there is no ranking/explanation for why these are top. |
| 127 | TRAN - Earnings Call Transcripts | news | Sidebar says `TRAN Earnings Call Transcripts`, but opened pane/help says `TRAN · Trade blotter`. | Refresh trades returns `OK`; trade blotter has `0 row(s)`, symbol filter, disabled CSV, and `portfolio.db has 0 row(s)`. | Wrong function is wired to the code. The transcript feature is not reachable; users get a trade blotter instead of earnings-call transcripts. |
| 128 | TRQA - Transcript QA | news | Help says run a list of questions against an earnings call transcript or audio. | Run returns `DEGRADED`; questions `What changed?` and `What are the risks?` both answer `-`, model `none`, tokens/cost `0`, sources `WHISPER LLM`. | No transcript/audio selector, no question editor beyond hidden Advanced, and no real answer is generated. The cost chart is a flat zero line and degraded state gives no actionable failure. |
| 129 | TSAR - Transcript Search | news | Help says search across stored earnings call transcripts using FTS5. | Run returns `DEGRADED`; query is fixed to `revenue`, status `ok`, source `TRANSCRIPTS_ARCHIVE`, but no hit rows appear. | No visible search box/query control in the pane. It returns no transcript matches and exposes only the baked-in query. |
| 130 | CSRC - Commodity Screener | screen | Help is generic global ROW/Advanced/Deep text. | Run returns `OK`; three baseline rows: `CL=F`, `GC=F`, `NG=F`; source `COMMODITY_SCREENER_BASELINE`. | Not a real screener: no commodity filters, price/change/volume/term-structure fields, or provider timestamp. ROW 50 still returns only three static rows. |
| 131 | EQS - Equity Screener | screen | Help says Universe `SP500`; visible DSL query area, three preset buttons, and universe field. | Run with default query `sector = Information Technology AND market_cap > 50_000_000_000` returns `OK` but `MATCHED 0`, elapsed 2ms. | Default query should match obvious large-cap tech names but returns nothing. No field dictionary, syntax validation, data freshness/source, or result explanation is shown. |
| 132 | FRH - Funding Rate Heatmap | screen | Help says perpetual funding rates across Binance/Bybit/OKX, top 25 pairs. | Run returns `OK`; three symbols `ETHUSDT`, `BTCUSDT`, `SOLUSDT` with Binance/Bybit/OKX rates and avg; sources `BINANCE BYBIT OKX`. | Real data appears, but only 3 pairs are shown despite top-25/ROW 50. The chart is a line over Binance values, not a heatmap, and there are no exchange/pair/time controls. |
| 133 | FSRC - Fund Screener | screen | Help is generic global ROW/Advanced/Deep text. | Run returns `OK`; two rows `SPY` and `QQQ` with AUM values; source `FUND_SCREENER_BASELINE`. | Static baseline, not a fund screener. No expense ratio, asset class, issuer, performance, holdings, filters, or data source timestamp. |
| 134 | ICX - Index Constituents | screen | Help says major equity index constituents from Wikipedia-backed cache. | Run returns `OK`; only `AAPL`, `MSFT`, `NVDA` with sector; source `WIKIPEDIA`. | Incomplete constituents list and no index selector. ROW 50 still returns three rows, and weight, market cap, rebalance date, and source link are missing. |
| 135 | MAP - World Market Heatmap | screen | Help says MSCI single-country ETF day-change heatmap for 25+ countries. | Run returns `NEEDS DATA`; `provider_unavailable`, `yfinance world ETF quotes unavailable`, and next action suggests deterministic template. | Core heatmap does not render. No country grid, no fallback button for deterministic mode, and no provider recovery/status guidance beyond text. |
| 136 | MICRO - Market Microstructure | screen | Help says order-book depth, imbalance, spread, and Kyle's lambda proxy. | Run returns `DEGRADED`; Binance depth rows for levels 5/10/20/50, best bid/ask, spread, top10 imbalance, `kyle_lambda_proxy 0`; source `BINANCE`. | Useful order-book data exists, but symbol is implicit and not editable in normal UI. Degraded warning says `ok`, Kyle lambda equation is not shown, and there is no depth ladder or bid/ask visualization. |
| 137 | MOSS - Most Volatile | screen | Help says realised-volatility leaderboard across watchlist or universe. | Run returns `OK`; 11 rows from YFinance with annualized vol, samples 81, last close; top visible rows include META, TSLA, NVDA, MSFT. | Data loads, but universe/watchlist and lookback controls are not exposed. The line chart is just leaderboard row order, not volatility history or distribution. |
| 138 | MOST - Most Active | screen | Help says sorted-by-volume function; visible sort tabs, asset-class tabs, and Refresh exist. | Refresh returns `OK`; six rows BTCUSDT, ETHUSDT, TSLA, NVDA, AAPL, MSFT with last, percent change, volume, dollar volume. | Looks hardcoded/sample-like: names/classes are blank, prices are rounded/static-looking, no source strip/provider timestamp is visible, and only six rows are returned for ROW 50. |
| 139 | SECF - Security Finder | screen | Help is generic global ROW/Advanced/Deep text. | Run returns `OK`; nine mixed rows including AAPL/MSFT plus BTCUSDT/EURUSD/GC=F and `Template` rows for ETHUSDT/SOLUSDT; chart `rows · marketCap`. | Security universe is fake/mixed: crypto, FX, commodities, and template rows all get equity-style market-cap/fundamental columns. No real search field, filters, or provider freshness. |
| 140 | SECT - Sector Heatmap | screen | Help says S&P 500 sector ETF day/MTD/QTD/YTD heatmap. | Run returns `NEEDS DATA`; `provider_unavailable`, yfinance sector ETF quotes unavailable, source `NO_LIVE_SOURCE`. | No sector heatmap renders. There is no fallback view, no ETF list, no provider timestamp, and no selectable period despite help promising day/MTD/QTD/YTD. |
| 141 | SRCH - Bond Screener | screen | Help is generic global ROW/Advanced/Deep text. | Run returns `OK`; two baseline rows `US10Y` and `US2Y` with yields/durations; source `BOND_SCREENER_BASELINE`. | Static two-row baseline. No maturity/issuer/rating/currency/yield filters, no bond identifiers, and no real screener universe. |
| 142 | WEI - World Equity Indices | screen | Help says refreshes every 30s; tabs exist for All, Americas, Europe, Asia, and MEA. | Refresh returns `OK` but `0 row(s)`, empty state `No quotes`, elapsed about 3.7-4.8s. | Broken empty output masked as OK. No provider/source/error message, no index quotes, no chart, and region tabs cannot surface data. |
| 143 | AGENT - Symbol Agent | native | Help says all-function ranker; visible candidate list and per-function timeout controls. | Run Agent returns `OK`; 141 functions, best `BTCUSDT`, score `+0.207`, pass 141/fail 0, visible evidence row `MLSIG portfolio +0.207 50% signal`; method `all_function_symbol_agent_v3_fast_probe`. | This now produces scores, but explainability remains thin: only one evidence row is shown for a 141-function score, confidence is fixed-looking, and the UI does not show which three functions are excluded from the 144-function catalog. |
| 144 | ASK - Ask Agent | native | Help says Planner, Search, Summarize, Viz; visible prompt box and prompt chips exist. | Asked `morning briefing - what should I watch`; result `OK` in about 15.9s with narrative, plan, portfolio branch, scan branch, news branch, and buttons to open PORT/SCAN/TOP. | Basic orchestration works, but provenance is weak. It shows `PLAN 0 ms`, `SUMMARIZE 0 ms`, `VIZ 0 ms`, a strange rationale `symbols: I`, no citations to exact underlying rows, and no way to inspect/modify the fanout plan before execution. |

## Coverage Summary

- Coverage: 144 / 144 functions manually opened and run in the desktop ShowMe UI.
- Runtime under test: `/Applications/showMe.app`, sidecar `127.0.0.1:64864`, `HEALTHY`.
- Highest-priority bugs: `TRAN` opens the wrong function, `NSE` throws a raw Python exception, `WEI` returns an empty `OK` state, `MAP/SECT/SAT` do not render promised visual outputs, many screeners return baseline/template rows, and many charts plot row order instead of meaningful market/time dimensions.
- `AGENT` now returns scores in the tested packaged app, but evidence is still too thin for a 141-function ranker.
- `ASK` runs and creates a briefing, but plan/fanout provenance is weak and some timing fields are zero.

## Debug Progress

| Status | Function(s) | Bug fixed | Live verification |
| --- | --- | --- | --- |
| fixed | `PORT` | Old paper/legacy positions had no close workflow and could re-import after removal. Added dry-run preview and explicit close API/UI with closed-position suppression. | `/fn/PORT` showed `Preview`/`Close`; `Preview` on the first row returned qty/notional/realized P&L/remaining positions without deleting state. |
| fixed | `AIM`, `BBGT`, `EMSX`, `FXGO`, `TSOX`, `TCA`, `EXEC` | Trade screens returned misleading `OK`/empty/synthetic values, missing ticket controls, wrong bond asset class, and filled execution parents still appeared live. | `BBGT` showed SIDE/QTY/TYPE/TIF and quantity validation; `AIM` showed `EMPTY`; `TSOX` used BOND `US10Y`; `TCA` showed empty-order methodology; `EXEC` showed `needs_close` and ISO timestamps. |
| fixed | `AGENT` | Scores/evidence were too thin and excluded functions were invisible. Added transparent local signal probes, catalog count, excluded function list, methodology, and multi-row evidence. | `/fn/AGENT` showed catalog `144`, functions `141`, excluded `AGENT/ASK/HOME`, best nonzero score, and 11 evidence rows. |
| fixed | `MAP` | No heatmap, no fallback mode, and provider failures were text-only. Added native heatmap pane, Live/Model control, country ETF grid, source labels, and truthful provider/model status. | `/fn/MAP` live showed 14 live ETF rows from yfinance; Model showed 25 deterministic rows, source `world_market_model`, and no provider-unavailable status. |
| fixed | `SECT` | No heatmap and no day/MTD/QTD/YTD controls. Added native sector heatmap pane, Live/Model mode, period controls, ETF table, and period-aware model fallback. | `/fn/SECT` Model `YTD` showed 11 sectors, YTD header, scaled YTD changes, and source `sector_heatmap_model`. |
| fixed | `WEI` | Empty `OK` state masked quote-provider failure; live rows lacked visual summary, names, regions, and true point change. Added fallback status, performance strip, live row enrichment, `prev_close`, true `change`, regions, and LIVE state. | `/fn/WEI` showed 11 live index rows, names such as S&P 500/Nasdaq Composite, AMERICAS/EUROPE/ASIA/MEA regions, true `Δ`, `Δ %`, day ranges, and LIVE tags. |
| fixed | `NSE` | Raw Python exception and poor default query. RSS slicing exception was removed earlier; default query now uses `bitcoin`, which returns visible results in packaged app. | `/fn/NSE` showed query field `bitcoin`, 5 news articles, timestamps, `newsbtc.com` source, and source strip `SQLITE_FTS`; no Python exception. |
| fixed | `TRAN` | Code opened the wrong native trade blotter pane. Registry now lets `TRAN` use the transcript backend/stub. | `/fn/TRAN` opened Earnings Call Transcripts and showed Apple Q2 2026 Seeking Alpha transcript link; no trade blotter. |
| fixed | `TSAR` | Query box existed after prior fix, but the initial auto-run used the previous pane's stale query. FunctionStub now runs new codes with the new default query override. | `/fn/TSAR` showed query `revenue guidance` and result reason for the same `revenue guidance` query, plus ingest/broader-query next actions. |
| fixed | `EQS` | Default DSL used backend-invalid fields/values and returned zero matches; universe parsing also sent `SP500` as a character list. Updated presets, normalized result parsing, and sent the benchmark universe correctly. | `/fn/EQS` showed default query `sector = "Technology" AND marketCap > 50000000000`, `MATCHED 2 / SCANNED 8`, source `EQUITY_SCREENER_MODEL`, and AAPL/MSFT rows. The PE/Beta preset changed the query and preserved the result table. |
| fixed | `CSRC` | Commodity screener was a three-row static stub with no filters, prices, contract scale, source state, or field explanation. Added commodity reference universe, DSL filter support, live quote merge, contract units, open interest, and field dictionary. | `/fn/CSRC` showed query `sector = "Energy"`, 3 Energy contracts (`CL=F`, `BZ=F`, `NG=F`), live last/high/low/volume columns, source count 2, and contract units. |
| fixed | `FSRC` | Fund screener was a two-row AUM-only baseline. Added ETF/fund universe, expense/AUM/category/holdings fields, DSL filter support, live quote merge, and field dictionary. | `/fn/FSRC` showed query `expenseRatio < 0.01 AND aum_usd > 10000000000`, 10 fund rows, live prices/volume/change_pct, and bar chart `rows · change pct`. |
| fixed | `SRCH` | Bond screener was a two-row baseline and its first live fix exposed a bad chart metric (`tenor_years`). Added bond reference universe, yield/duration/rating/maturity filters, and made charts prefer `yield`. | `/fn/SRCH` showed query `yield >= 4 AND duration <= 10`, 5 bond rows (`US3M`, `US2Y`, `US5Y`, `US10Y`, `GB10Y`), ratings and durations, then after rebuild the chart showed `rows · yield`. |
| fixed | `SECF` | Security finder reused EQS and mixed asset classes into equity market-cap columns. Replaced it with a security-master text search that returns symbol/name/asset_class/exchange/country/sector/tags without equity-only fields. | `/fn/SECF` showed query `technology`, 3 matched rows (`AAPL`, `MSFT`, `NVDA`), asset class `EQUITY`, tags, `match=all_terms`, scanned 13, and source `SHOWME_SECURITY_MASTER`. |
| fixed | `ICX` | Index constituents had no index control and fell back to three rows. Added an Index control mapped to backend `index`, larger reference rows, field dictionary, source URL, and truthful provider-unavailable state when Wikipedia cannot be normalized. | `/fn/ICX` showed Index `SPX`, 11 constituent rows from the reference fallback, source URL for Wikipedia, `NEEDS DATA` warning explaining provider normalization failure, and next actions for supported index codes. |
| fixed | `SAT` | Satellite pane dropped Sentinel Hub errors and showed metadata as if the function were usable; it also had no normal bbox/date controls and rendered no imagery/preview. Added bbox parsing, Range-to-date wiring, truthful provider-unavailable state, next actions, and generic media rendering for true PNG or bbox preview. | `/fn/SAT` showed BBox field and Range buttons; selecting `1M` produced `date_from 2026-04-03` and `date_to 2026-05-03`. It rendered a labeled `BBox preview (not satellite imagery)` and clearly reported missing `SENTINELHUB_CLIENT_ID/SECRET` instead of pretending to show Sentinel-2 imagery. |
| fixed | `TRQA` | Transcript Q&A had no normal transcript input and accepted empty `-`/`none` LLM responses as successful answers. Added a visible transcript textarea, mapped the visible query into a single question, and falls back to local extractive Q&A with evidence when the LLM/router returns an unusable answer. | `/fn/TRQA` accepted pasted transcript text, ran the visible query `What changed in guidance?`, and returned one answer/evidence row: `CEO: We raised full-year guidance to 8 percent revenue growth because cloud demand is stronger.`, confidence `0.59`, model `local_extractive`, cost/tokens `0`. |
| fixed | `AV` | Audio/video archive returned static/sample rows with no playable links or dates. It now queries live podcast RSS feeds, extracts enclosure media, uses playable audio URLs for title links, includes source/published/summary/status fields, and returns `provider_unavailable` with next actions when no playable media is found. | `/fn/AV` with query `market news` showed `OK OBJECT 50`, 50 rows, source `PODCAST_RSS`, `Planet Money` items, relative dates, and title links pointing to playable `.../audio/128/default.mp3...` URLs instead of empty/sample links. |
| fixed | `BRIEF` | Daily brief returned `STRING`, `0 ROWS`, `WATCHLIST_CACHE`, and no visible brief. It now calls live news, returns structured `markdown` plus article rows, strips raw HTML from summaries, and renders the daily brief as a readable top panel before the article list. | `/fn/BRIEF` showed `OK OBJECT`, source `RSS`, `ShowMe Daily Brief — 2026-05-03`, watchlist `AAPL, MSFT, BTCUSDT`, matched symbols `AAPL, BTCUSDT, MSFT`, and linkable top-story bullets instead of an empty cache string. |
| fixed | `EVTS` | Corporate Events emitted a fake `earnings/date -/no_event_feed` placeholder and had the wrong generic query control. It now flattens only dated provider events, returns `provider_unavailable` with next actions when none exist, and uses a symbol-first panel with equity-only suggestions. | `/fn/EVTS` opened with `Symbol AAPL`, quick chips `AAPL/NVDA/MSFT`, source `YFINANCE`, and `NEEDS DATA` explaining no dated corporate events were returned. No `date -` placeholder row appeared. |
| fixed | `SOSC` | Social Sentiment returned an `AAPL discussion placeholder` and hid provider reality. It now returns real provider rows only, exposes Stocktwits/Reddit provider warnings, uses symbol-first equity controls, separates Reddit post score from sentiment score, and charts `sentiment_score` instead of raw post upvotes. | `/fn/SOSC` opened with `Symbol AAPL`, quick chips `AAPL/NVDA/MSFT`, showed real Reddit post links, surfaced Stocktwits `403 Forbidden` as a degraded warning, and the chart title changed from `rows · score` to `rows · sentiment score`. |
| fixed | `CN` | Company News matched broad RSS feed/source labels as article content, causing false AAPL relevance. Matching now ignores feed/source text, filters direct symbol/alias matches, and strips markup. | `/symbol/AAPL/CN` showed 7/100 Apple/AAPL-relevant rows such as Apple earnings/dividend headlines with impact reasons/source links; previous Altria/OpenAI/Nvidia false matches disappeared. |
| fixed | `NALRT` | Critical alerts could be raised from stale headlines. Added alert freshness capping, explicit scoring methodology, feed health details, and stale-window reasons. | `/fn/NALRT` on `BTCUSDT` showed `alert_count 0`, `freshness_max_hours 48`, method `deterministic_impact_score_v2`, stale rows labelled `stale for alert window >48h`, and chart axis `published_at / importance_score`. |
| fixed | `NI` | News by Topic was routed like symbol news and had no real topic control. It now has a native Topic input/preset pane and preserves topic text instead of converting it to a ticker. | `/fn/NI` showed `Topic` controls and presets; `CRYPTO` returned topic rows with footer `topic · CRYPTO`, no symbol bar. |
| fixed | `READ` | Personalized News mixed cache/unavailable placeholders, had no watchlist/time controls, and charted news like row-order data. It now sends visible Watchlist to backend `watchlist`, adds Range-based freshness filtering, removes stale/unavailable rows, and titles time charts by `published_at`. | `/fn/READ` showed Watchlist `AAPL, MSFT, BTCUSDT`, Range `1M`, 23 RSS rows, no `WATCHLIST_CACHE`/unavailable rows, no 3-month critical item, and chart title `published at · importance score` over `Apr 30` to `May 03`. |
| fixed | `TLDR` | Daily TLDR had no visible watchlist, blank/incorrect movers, and no model provenance. It now has a Watchlist field, quote-driven mover chart, `summary_model`, `quote_count`, `mover_count`, and separates gainers from decliners. | `/fn/TLDR` showed Watchlist `AAPL, MSFT, BTCUSDT`, quote chart `symbol / change_pct`, `summary_model local_deterministic_tldr_v2`, `quote_count 3`, `mover_count 3`, `Top movers up: AAPL (+3.32%), MSFT (+1.51%)`, and `Top movers down: BTCUSDT (-0.18%)`. |
| fixed | `TOP` | Top News lacked query/time controls, included stale headlines, and showed unexplained/raw snippets. It now has Query presets, visible Query field, Age control, backend freshness filtering, stripped summaries, impact scores, and ranking reasons. | `/fn/TOP` market showed 50/50 fresh headlines with impact badges and source links. Crypto preset with `AGE 45D` showed 1/50 fresh headline and no 1-year-old WSJ row; footer showed `age · 45d`, `query · crypto`, `sources · rss`. |
| fixed | `FRH` | Funding Rate Heatmap defaulted to the agent's three peer symbols, had no normal pair selector, and the generic chart renderer picked a bar/line chart over `rows` instead of a heatmap. It now lets the backend use the top-25 default unless explicit symbols are sent, exposes a visible Symbols field, returns `surface`/`rate`/methodology data, and prioritizes `surface` charts. | `/fn/FRH` showed Symbols populated with 25 crypto perpetual pairs, `OK OBJECT 25 ROWS`, sources `BINANCE BYBIT OKX`, `HEATMAP surface · rate`, green/red rate tiles, Binance/Bybit/OKX table columns, and provider counts. |
| fixed | `MICRO` | Market Microstructure opened with an implicit/recent equity symbol, returned fake proxy data for unsupported assets, hid the book-depth methodology, and had no real depth visualization. It now opens as a crypto symbol-first pane, defaults and quick-picks to crypto perpetual symbols, returns `NEEDS DATA` for unsupported equity L2 instead of proxy values, and exposes live Binance depth buckets plus Kyle lambda methodology/scaled metric. | `/fn/MICRO` first showed `AAPL` as unsupported with `NEEDS DATA` and no proxy source; after the crypto-default fix it opened on `BTCUSDT`, showed quick picks `BTCUSDT/TONUSDT/DOGEUSDT/ETHUSDT/SOLUSDT`, source `BINANCE`, `HEATMAP surface · value`, top-5/10/20/50 bid/ask notional and imbalance rows, spread metrics, and no `microstructure_proxy` source. |
| fixed | `MOSS` | Most Volatile had no visible universe/lookback controls and charted leaderboard row order. It now exposes a Universe field and Range controls, computes live annualized realized volatility for the selected universe, returns field definitions/methodology, and charts rolling volatility history for the top symbol. | `/fn/MOSS` showed Universe `AAPL, TSLA, NVDA, META, AMZN, MSFT, GOOGL, BTCUSDT, ETHUSDT, SOLUSDT`, Range controls, source `YFINANCE`, `vol_annualized`, `vol_pct`, `samples`, `start/end` rows, and `TIME SERIES history · vol`; with `1M` selected it recomputed 19/30-sample date windows and kept the history time-series chart instead of `rows · vol`. |
| fixed | `MOST` | Most Active looked like a six-row/static sample, asset tabs were not preserved by the route layer, and the pane lacked provider/methodology context or a visual activity ranking. It now preserves `All/Equities/Crypto/FX` filters, returns structured live quote rows enriched with name/class/venue/source/as-of/field dictionary, and renders ranked activity bars above the table. | `/fn/MOST` All showed 11 live rows, source `YFINANCE + SHOWME_MOST_ACTIVE_UNIVERSE`, methodology/as-of cards, ranked volume bars, live state pills, and name/class/venue columns. Clicking `Crypto` filtered to 6 Binance crypto rows; clicking `FX` filtered to 4 FX pairs with only FX class rows. |
| fixed | `ASK` | Ask Agent briefing could misparse `I` as a ticker, show zero-looking phase timings, hide branch provenance, and hang while waiting for the full scanner branch. It now ignores pronoun stopwords in ticker extraction, uses a bounded labelled fast briefing scan inside fanout, times out slow branches, and renders an Evidence section with function/source/row references. | `/fn/ASK` ran `morning briefing - what should I watch` without `symbols: I`, completed instead of staying on `Thinking...`, showed readable phase timings, rendered portfolio/scan/news branches, and displayed Evidence refs for `PORT`, `SCAN` (`showme_scanner_reference_model` / `fast_briefing_model`) and `TOP` with row counts/top items. |
| fixed | `GEX`, `HVT`, `IVOL`, `OMON`, `OSA`, `OVME` | Derivative functions hid equations/assumptions, lacked normal controls, and often charted row-order or placeholder option data. Added live option-chain shaping for GEX/IVOL/OMON, HVT rolling realized-vol history with Range control, visible OVME/OSA option assumption controls, Black-Scholes/realized-vol/GEX methodology and field dictionaries, plus strike/expiry/vol/GEX/payoff chart data. The generic renderer now prefers numeric `curve` payloads and renders methodology/field dictionaries as first-class result panels. | `/fn/GEX` showed 45 live option-chain strikes, `source_mode live_chain`, gamma flip/call wall/put wall, and a strike GEX bar chart. `/fn/HVT` showed Range controls, yfinance rolling `history · vol pct`, 30/60/90/252D rows and formula text. `/fn/IVOL` showed 263 live option-chain rows and `HEATMAP surface · vol pct`. `/fn/OMON` showed selected expiry `2026-05-04`, 78 CALL/PUT rows, and an IV heatmap instead of expiry-only placeholders. `/fn/OVME` showed visible Type/Spot/Strike/T/Vol/Rate controls, `curve · price`, Black-Scholes methodology, field dictionary, and `black_scholes_formula`. `/fn/OSA` showed readable Strategy/Spot/Long K/Short K/T/Vol/Rate controls, `curve · pnl`, net debit/max gain/max loss, methodology, field dictionary, leg table, and `BLACK_SCHOLES_FORMULA`. |
| fixed | `MLSIG`, `BLAK`, `BMTX`, `BTFW`, `BTUNE` | Portfolio model/backtest functions had hidden symbol/universe/strategy/horizon inputs, weak degraded explanations, and missing or row-order charts. Added symbol routing for MLSIG/BTFW/BTUNE, visible Universe controls for BLAK/BMTX, Strategy/Horizon controls, live/reference methodology and field dictionaries, feature-importance rows, Black-Litterman weight rows, backtest matrix surfaces, tuning parameter labels, and chart priorities for importance/weights/return surfaces. Fixed a live BTFW mismatch where the UI showed `SMA` but initial results used `buy_and_hold`. | Latest packaged app `/Applications/showMe.app`, `HEALTHY : 57530`, showed `/fn/MLSIG` with `12 ROWS`, horizon controls, `source_mode live_yfinance`, `BAR CHART rows · importance`; `/fn/BLAK` with Universe `AAPL, MSFT, NVDA`, 3 rows, `BAR CHART rows · market weight`, Black-Litterman methodology; `/fn/BMTX` with Universe `SPY, QQQ, IWM, AAPL, MSFT, TSLA, NVDA, AMZN`, Strategy `All/SMA/RSI/Buy-hold`, 24 runs, `HEATMAP surface · total return`; `/fn/BTFW` with Symbol `AAPL`, Strategy `SMA`, result `strategy sma_crossover`, 250 rows, equity curve; `/fn/BTUNE` with Strategy `SMA`, 20 combos, labelled parameter heatmap, best Sharpe/return/Calmar, and methodology. |
| fixed | `ACCT`, `CORR`, `GREEKS`, `LOTS`, `MARS`, `MGN`, `PCAS`, `PFA`, `PORT_OPT`, `PORT_WHATIF`, `PSC`, `PVAR`, `REBA`, `RPAR`, `STRS`, `TLH`, `TRA` | Portfolio risk/workflow functions had hidden or blocking inputs, synthetic fallback leakage, row-order charts, weak methodology, and opaque portfolio workflows. Added visible Universe/scenario/action/target/tax/sizing controls, live date-aligned return joins for mixed equity/ETF/crypto/FX/commodity universes, meaningful matrix/frontier/risk/stress/sizing/attribution rows, methodology and field dictionaries, and chart priorities for correlations, efficient frontiers, risk contribution, P&L, drift, and effects. Fixed STRS blocking by making live quote refresh opt-in and charting stress `total_pnl` instead of horizon days. | Latest packaged app `/Applications/showMe.app`, `HEALTHY : 58757`, showed `/fn/CORR` source `YFINANCE`, 6-symbol heatmap, 249 samples, methodology; `/fn/PORT_OPT` source `YFINANCE`, 9 symbols, 249 samples, `CURVE efficient frontier · return` with `vol / return`; `/fn/MARS` source `YFINANCE`, 662 samples, 6 factor loading rows, methodology; `/fn/STRS` completed in ~2 ms with 4 scenarios, 51 positions, price source `portfolio_state_cost`, `BAR CHART rows · total pnl`, stress P&L table, methodology and field dictionary. API checks on port `58757` also returned OK for `RPAR`, `PCAS`, `PVAR`, `PFA`, `PSC`, `PORT_WHATIF`, `REBA`, `TLH`, `MGN`, `GREEKS`, `ACCT`, and `TRA`; `LOTS` truthfully returned empty local tax-lot ledger with methodology. |
| fixed | `BQL`, `BQUANT`, `DAPI`, `FLDS`, `ISIN` | API functions had generic/static output, misleading nonexistent `/api/v1/*` route examples, missing query/field/id controls, nested or non-tabular responses, weak methodology, and one false notebook-ready state. Added visible BQL/endpoint/field/identifier controls, BQL range/row controls, ID Type control for ISIN, actual mounted sidecar endpoint manifest for DAPI, field catalog rows/examples for FLDS, flattened OpenFIGI match rows for ISIN, methodology and field dictionaries, and truthful BQUANT `not_configured` state instead of a fake notebook capability. Also removed the misleading ISIN rank chart by making provider rank a label rather than a numeric metric. | Latest packaged app `/Applications/showMe.app`, `HEALTHY : 59205`, showed `/fn/BQL` with BQL query field `get(close, volume) for(['AAPL','MSFT']) by(date)`, Range/Rows controls, source `YFINANCE`, 100 rows, `symbols 2`, time-series chart `history · close`, and methodology. `/fn/DAPI` showed Endpoint filter `quote`, actual `/api/quote/{symbol}` row, mutation flag, request/response shape, methodology, and source `SHOWME_FASTAPI_ROUTES`. `/fn/FLDS` showed Field search `price`, 3 catalog rows (`price`, `pb`, `ps`), examples, methodology, and source `SHOWME_FIELD_CATALOG`. `/fn/ISIN` showed Identifier `AAPL`, ID Type `AUTO`, 73 OpenFIGI matches, flattened table rows with rank labels `#1`, FIGI/ticker/exchange fields, no meaningless rank chart, methodology, and source `OPENFIGI`. `/fn/BQUANT` showed `NEEDS DATA`, `not_configured`, 3 readiness rows, notebook URL `/notebook`, next actions, methodology, and source `LOCAL_NOTEBOOK_MANIFEST`. API checks on port `59205` returned `BQL ok rows 80 symbols ['AAPL', 'MSFT']`, `DAPI ok`, `FLDS ok`, `ISIN ok rank #1 ticker AAPL`, and `BQUANT provider_unavailable` with the no-mounted-Jupyter reason. |
| fixed | `ALLQ`, `CRPR`, `CRVF`, `DDIS`, `DEBT`, `GC3D`, `SRSK`, `TAUC`, `WB`, `YAS` | Bond functions had missing issuer/country/security controls, row-index charts, placeholder/fallback ambiguity, blocked YAS asset-class routing, and weak methodology/formula disclosure. Added visible bond/issuer/country/countries/action/type/recovery/price/coupon/benchmark controls, labelled fallback/source modes, methodology and field dictionaries, bid/ask quote fields, rating dates/rationales, maturity buckets with units, macro-vs-portfolio debt disclosure, true CRVF tenor curve, GC3D surface heatmap priority, SRSK PD formula, TAUC real ISO dates/fallback labelling, WB country yield heatmap, and YAS percent/decimal rate normalization plus bond quick-picks. | Latest packaged app `/Applications/showMe.app`, `HEALTHY : 59813`, was manually checked with Computer Use. `/fn/ALLQ` showed Bond/Mid/Spread/Size controls, composite/TRACE proxy rows with bid/ask/size/quote_time/spread_bps and methodology. `/fn/CRPR` showed Issuer control, S&P/Moody's/Fitch rows with rating_date/rationale and public-default source disclosure. `/fn/CRVF` showed Country `US`, source `FRED`, 8 tenors, `CURVE curve · yield`, x-axis `tenor_years / yield`, and methodology. `/fn/DDIS` showed Issuer control, maturity-bucket bar chart `rows · amount usd bn`, USD bn units and methodology. `/fn/DEBT` showed Countries control, `HEATMAP rows · debt to gdp`, `portfolio_linked no`, and macro-baseline disclosure. `/fn/GC3D` showed Country and Range controls, `HEATMAP surface · yield`, date-tenor cells such as `Apr 01 · 10Y`, 30 points, and surface methodology. `/fn/SRSK` showed Countries/Recovery/Fallback spread controls, formula `PD ~= spread / (1 - recovery)`, PD heatmap and FRED-key warning instead of silent fake data. `/fn/TAUC` showed Rows/Range plus Action/Type controls, live `treasurydirect` run with 3 ISO-dated auction rows and methodology; a later API run fell back to `treasury_auction_fallback` with current-date ISO rows and labelled `source_mode`. `/fn/WB` showed Countries control, 8 10Y country yields, `HEATMAP rows · yield`, and field dictionary. `/fn/YAS` opened on `US10Y` instead of stale `AAPL`, quick-picks `US10Y/US2Y/US30Y`, Price/Coupon/Years/Freq/Benchmark controls, normalized `coupon_pct 4.25`, `benchmark_pct 4.45`, `ytm_pct 4.312082`, `spread_bps -13.792` by API, and `CURVE curve · price`. API checks on port `59813` returned OK rows for all 10 functions: `ALLQ 3`, `CRPR 3`, `CRVF 8`, `DDIS 4`, `DEBT 4`, `GC3D 30`, `SRSK 4`, `TAUC 5 fallback-labelled`, `WB 8`, `YAS 5`. |
| fixed | `CHGS`, `GP`, `HP`, `TECH` | Chart functions had broken or generic technical output, no visible study controls, missing HP chart, row/date pseudo charts, blank elapsed values, and non-obvious run controls. TECH now returns OHLCV, indicator rows, overlay series, RSI/MACD/ATR/ADX formulas, methodology and field dictionary. CHGS now calls the live TECH payload by default from the generic pane. HP renders a real candlestick/volume chart above its OHLCV table. GP/HP share searchable symbol suggestions, visible `Run` buttons, and client-side elapsed fallback when the backend omits timing. | Latest packaged app `/Applications/showMe.app`, `HEALTHY : 60284`, was manually checked with Computer Use. API checks on the packaged backend returned `GP rows 62 ohlcv 62 sources yfinance`, `HP rows 62 ohlcv 62 sources yfinance`, `TECH rows 120 ohlcv 120 indicators sma_20/sma_50/ema_20/bb_* indicator_rows 4`, and `CHGS rows 120 ohlcv 120 indicator_rows 4`. `/fn/TECH` showed Symbol, Range, RSI/SMA/EMA/Bollinger controls, `Run`, `CANDLESTICK ohlcv · OHLC`, summary cards, yfinance source, and TECH methodology/formulas. `/fn/CHGS` showed the same live technical-study payload instead of template rows. `/symbol/AAPL/HP` showed `62 bars`, real OHLCV candlestick/volume chart, CSV enabled, and `elapsed · 777 ms`. `/symbol/AAPL/GP` showed visible `Run`, 250-candle yfinance price chart, source `yfinance`, and `elapsed · 756 ms`. |
| fixed | `WATCH` | Watchlist had no normal symbol suggestions, no price-history visualization, provider failures could look like live blank rows, and the footer counted failed quotes as live. Added datalist suggestions, 30D mini sparklines from live GP history, explicit `ERROR / quote error` row state, remove/undo state handling, and a footer that excludes quote-error rows while showing `errors · N`. | Latest packaged app `/Applications/showMe.app`, `HEALTHY : 62405`, was manually checked with Computer Use. `/fn/WATCH` showed Add field suggestions, AAPL/MSFT/GOOGL 30-point sparkline images, live polling quote rows for AAPL/MSFT/GOOGL/BTCUSDT/ETHUSDT, and `PENGUUSDT` as `err`, `ERROR`, `quote error`. Footer changed from misleading `6 / 6 live` to `5 / 6 live errors · 1 idle`. Remove/Undo was code-built but not destructively clicked during audit because it mutates the local watchlist. |
| fixed | `MEET`, `PEOP` | Communication functions returned either fake/empty meeting placeholders or hid useful rows behind warning cards. PEOP now searches the local people directory first and falls back to public-reference people with source URLs/contact-status disclosure. MEET now builds participant, note, news, portfolio, connection-status, methodology, and field-dictionary rows without fake meeting notes. The generic renderer now keeps mixed briefing tables as DataGrid rows instead of dropping participant rows into a news-card view. | Packaged app live checks with Computer Use showed `/fn/PEOP` returning Arthur Levinson, John Ternus, and Tim Cook rows for `Apple management`, with Apple source URLs, `public_profile_only`, methodology, and field dictionary. `/fn/MEET` showed the visible Query field, Notion/GDELT configuration warnings, participant rows for Arthur Levinson/John Ternus/Tim Cook, explicit `meeting_note not_configured_or_empty`, `news not_available`, `portfolio not_linked`, connection methodology, and no stale/fake Satoshi or placeholder briefing row. API checks returned `PEOP rows 3 source people_public_reference` and `MEET rows 6 sources granola, people_public_reference`. |
| fixed | `BGAS`, `BOIL`, `CPF`, `GLCO`, `NGAS`, `WETR` | Commodity functions used flat or misleading placeholder outputs, weak contract/source disclosure, row-index charts, hidden or wrong controls, and weather/forecast fallbacks that looked live. Added contract/benchmark/scenario/horizon/weather controls, live yfinance futures rows with OHLCV histories, labelled reference forecast buckets, commodity mover bars, source modes, methodology, field dictionaries, OpenWeather credential warnings, and removed the wrong WETR range selector in favor of a Days field. | Latest packaged app `/Applications/showMe.app`, `HEALTHY : 49509`, was rebuilt and manually checked with Computer Use. API checks returned `BGAS rows 1 history 252`, `NGAS rows 1 history 252`, `BOIL rows 2 history 252`, `CPF rows 16 surface 16 bucket 2025 WTI crude oil`, `GLCO rows 7+ live_yfinance`, and `WETR provider_unavailable rows 7 history 7`. Browser check of `/api/fn/CPF` showed year+commodity buckets in JSON. `/fn/BGAS`, `/fn/BOIL`, and `/fn/NGAS` showed Contract/Benchmark controls, true candlestick OHLC charts, last/min/max/delta cards, and methodology. `/fn/CPF` showed Commodities/Scenario/Horizon controls plus heatmap cells like `2025 WTI crude oil`, `2028 Copper`. `/fn/GLCO` showed sector tabs, ranked mover bars, contract/source columns, and live_yfinance rows. `/fn/WETR` showed Days/Location/Commodity/Lat/Lon controls, no range buttons, explicit `OPENWEATHERMAP_API_KEY not set`, and a labelled seasonal-model time series. |
| fixed | `ANR`, `APPL`, `BETA`, `CACT`, `DARK`, `DCF`, `DCFS`, `DDM`, `DES`, `DPF`, `DVD`, `EE`, `EREV`, `ESG`, `FA`, `FORM4`, `FTS`, `HDS`, `HFS`, `LITM`, `PIB`, `RV`, `SPLC`, `WACC` | Equity functions had hidden or weak symbol/provider/method controls, fabricated-looking unavailable states, stale native panes, missing formulas, row-index charts, and specialized UI that hid backend methodology. Added real yfinance/SEC/FINRA/Finnhub/OpenFIGI-style provider disclosure where available, transparent provider-unavailable states for stale FINRA and missing ESG vendor data, visible peer/form/date/rate controls, source/methodology/field dictionaries, true dividend/EPS/score/holder/relative-value/WACC charts, DCFS fair-value heatmap cells, DES native company field mapping, and FA formula/methodology rendering in its specialized pane. | Latest packaged app `/Applications/showMe.app`, `HEALTHY : 54443`, was rebuilt and manually checked with Computer Use. API checks returned all 24 equity functions without FAILS: `ANR 5`, `APPL 11`, `BETA 3`, `CACT 80`, `DARK provider_unavailable 1`, `DCF 5`, `DCFS 3 fair cells`, `DDM 5`, `DES 7`, `DPF provider_unavailable 1`, `DVD 85`, `EE 8`, `EREV 3`, `ESG provider_unavailable 4`, `FA 20`, `FORM4 40`, `FTS 6`, `HDS 24`, `HFS 3`, `LITM 1`, `PIB 50`, `RV 7`, `SPLC 4`, and `WACC 5`. Computer Use live checks showed `/fn/DES` with Apple company fields from yfinance, `DPF` and `ESG` as truthful `NEEDS DATA` states with reasons, `DVD`/`EE`/`EREV` as non-row-index charts, `FA` with income rows plus methodology/formula dictionary, `FORM4`/`FTS`/`HDS`/`HFS`/`LITM`/`PIB`/`RV`/`SPLC` with controls/evidence/source rows, and `WACC` with editable ERP/tax/risk-free/debt-cost/beta controls plus formula `WACC = (E/V * Re) + (D/V * Rd * (1 - tax))`. |
| fixed | `FRD`, `FXFC`, `FXH`, `FXIP`, `OVDV`, `WCRS` | FX functions had hard-coded or hidden pair/rate/tenor/vol inputs, row-index or missing charts, weak formula disclosure, incorrect hedge scenario direction, and WCRS returned `OK` with zero crosses. Added FX symbol-first routing and suggestions, visible spot/rate/tenor/vol/exposure controls, covered-interest-parity forwards, forward-carry forecast bands, hedge shock curve, FXIP candlestick history, labelled volatility-surface heatmap, WCRS cross-rate heatmap/table, methodology panels, field dictionaries, and source-mode disclosure. | Latest packaged app `/Applications/showMe.app`, `HEALTHY : 57739`, was rebuilt and manually checked with Computer Use. API checks returned no FAILS: `FRD rows 5 series 5`, `FXFC rows 4 series 4`, `FXH rows 1 series 5`, `FXIP rows 7 series 65`, `OVDV rows 25 series 25`, `WCRS rows 5 series 6`, with `WCRS USDEUR 0.92293493`. `/fn/FRD` showed `EURUSD`, FX quick suggestions, Spot/Base Rate/Quote Rate/Tenors controls, `CURVE curve · forward`, and CIP methodology. `/fn/FXFC` showed rate/vol/tenor controls and `CURVE curve · forecast`. `/fn/FXH` no longer used stale `USDT`; it showed EUR/USD notional/hedge/days/shock controls, different strengthens/weakens P&L, and `curve · total pnl`. `/fn/FXIP` showed ATM vol controls plus real OHLC candlestick history. `/fn/OVDV` showed parameter controls, `DEGRADED` warning for missing live OTC vol vendor, labelled reference source, 25-cell heatmap, methodology, and field dictionary. `/fn/WCRS` showed USD/EUR base tabs, cross-rate heatmap, table, methodology, and field dictionary; Browser check of `/api/fn/WCRS` also returned `status ok`, 5 rows, 6 series, source `reference_cross_rate_matrix`, and payload methodology/field dictionary. |
| fixed | `BTMM`, `COUN`, `ECFC`, `ECO`, `ECST`, `GMM`, `REGM`, `TRDH`, `WIRP` | Macro functions had hidden country/series/indicator/exchange/bank/symbol controls, nested provider payloads, row-index charts, missing methodology/formula disclosure, and misleading model/fallback source labels. Added structured rows/series/surfaces, visible macro controls, methodology and field dictionaries, BTMM search plus policy-rate history, ECO country/importance filters, ECST named series fallbacks, GMM normalized surprise scores, REGM symbol-first regime history, TRDH exchange-labelled hours heatmap, and WIRP central-bank probability equation disclosure. | Latest packaged app `/Applications/showMe.app`, `HEALTHY : 60952`, was rebuilt and manually checked with Computer Use plus Browser Use. API batch returned `FAILS []`: `BTMM rows 10 series 400`, `COUN rows 11 series 12`, `ECFC rows 10 series 10`, `ECO rows 1 series 1`, `ECST rows 3 series 3`, `GMM rows 3 series 3`, `REGM rows 4 series 160`, `TRDH rows 6 series 6`, `WIRP rows 4 series 12`. Computer Use showed BTMM controls/history, COUN `OK` with country input/methodology, ECFC country+indicator controls and reference forecast rows, ECO `All` to `High` filter changing 2 events to 1, ECST `UNRATE` changing chart values to `4.0/4.1`, GMM surprise-score heatmap, REGM SPY/QQQ/IWM/TLT quick symbols and history chart, TRDH exchange-labelled heatmap (`NYSE/NASDAQ/LSE/...`), and WIRP cut/hold/hike probabilities with no synthetic source marker. Browser Use opened `/api/fn/WIRP` on port `60952` and confirmed `source_mode reference_rate_probability_table`. |
| fixed | `ALRT`, `BIO`, `BMC`, `CDE`, `DINE`, `FLY`, `GRAB`, `LANG`, `ONCH`, `POLY`, `WHAL` | Misc functions had fake or hidden behavior: alerts lacked quote proof, biometric auth was backend-only, BMC was static, CDE hid evaluation results, restaurant/flight/whale/on-chain/prediction-market outputs could look fabricated or underexplained, screenshot email implied transmission, and language switching lacked state disclosure. Added visible controls, truthful provider states, real provider rows where available, methodology/field dictionaries, non-fabricated fallback handling, and CDE evaluate result rows. | Latest packaged app `/Applications/showMe.app`, `HEALTHY : 63004`, was rebuilt and manually checked with Computer Use plus Browser Use. API batch returned `FAILS []`: `ALRT ok`, `BIO rows 3`, `BMC rows 3`, `CDE_ADD ok`, `CDE_EVAL evaluation true`, `DINE rows 2 source openstreetmap_nominatim`, `FLY rows 5 source opensky`, `GRAB draft_only rows 2`, `LANG rows 12`, `ONCH rows 8 series 5 source mempool`, `POLY rows 7 source polymarket`, and `WHAL provider_unavailable rows 2 source whale_alert_not_configured`. Computer Use showed ALRT quote check `AAPL price 280.25 (yahoo_chart)` and preview, BIO LocalAuthentication capability rows without auto-prompting, BMC lesson/objective/example/quiz rows, CDE `operation evaluation` with `evaluation yes`, DINE OSM address/lat/lon rows with blank rating/price, FLY OpenSky callsign/altitude/speed rows, GRAB capture/email separation with `transmits_data`, LANG selected `tr` and reload state, ONCH BTC mempool chart with Glassnode auth warnings surfaced, POLY active non-closed markets with yes/no/outcome prices, and WHAL needs-data state explaining missing large-transfer feed instead of block-header substitutes. Browser Use opened `/api/fn/POLY` on the packaged runtime and confirmed `source_mode polymarket_gamma`. |
