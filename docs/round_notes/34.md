# Round 34 — Yekpare tek-ağaç refactor (2026-05-09)

## Özet

Üç-süreçli proje (`src-tauri/`, `src-ui/`, `src-py/`, `engine/`) tek bir
yekpare ağaca konsolide edildi. `engine/` artık ayrı bir Python ağacı değil,
normal bir `showme.engine` alt-paketi. `SHOWME_ENGINE_PATH` `sys.path`
enjeksiyonu kalktı.

## Yapısal değişiklikler

- `src-tauri/`   → `tauri/`
- `src-ui/`      → `ui/`
- `src-py/`      → `backend/`
- `engine/src/`  → `backend/showme/engine/`
- `engine/config` → `backend/config/`
- `src-py/tests` + `tests/` → `backend/tests/` + `tests/` (e2e)

## İmport rejimi

234 dosyada `from src.X` → `from showme.engine.X`. `attach_engine()` artık
sadece `SHOWME_ENGINE_ROOT` yayınlıyor; `sys.path`'a hiçbir şey eklemiyor.
PyInstaller `collect_submodules('showme')` ile tüm alt-paketleri otomatik
buluyor.

Doğrulama sırasında **2 ek import kalıntısı** yakalandı (ImportFrom-only AST
sweep'in `ast.Import` görmemesinden kaçtı):

- `backend/showme/engine/services/function_factory.py:171`
  `import src.functions as fns_pkg` → `import showme.engine.functions as fns_pkg`
  (ilk smoke'da `function_factory_warmed: false` + `ModuleNotFoundError("No module named 'src'")` ile yakalandı)

## Konfig güncellemeleri

- Root `package.json` workspace: `src-ui` → `ui`
- `sidecar:dev`: `cd backend && uv run python -m showme.server`
- `tauri/tauri.conf.json` `frontendDist`: `../ui/dist`
- `tauri/src/sidecar.rs` `default_sidecar_cwd` → `../backend`
- `tauri/src/commands.rs` migration cwd → `../backend`
- `backend/showme-backend.spec` yol-bağımsız (`Path(SPECPATH)`)
- `packaging/build_sidecar.sh`: sadece spec'i çalıştırır, çıktı `tauri/binaries/`
- **Yeni `@tauri-apps/cli` argüman sırası:** `package.json`'da
  `tauri --config X build` → `tauri build --config X` (CLI: "unexpected argument '--config'")
- **`backend/pyproject.toml` `[tool.ruff]`:** `extend-exclude = ["showme/engine"]`
  (engine alt-ağacı pre-refactor lint kapsamı dışındaydı; refactor onları
  kapsama dahil edince 190 pre-existing hata patladı, 125'i `--fix` ile
  oto-düzeltildi, kalan 65 stil hatası engine'in eski rejimi olarak
  bırakıldı)

## Doğrulama (P0–P6 sonuçları)

| Adım | Sonuç |
|---|---|
| P1 cleanup + deps | ✓ npm 364 paket, backend showme-backend 0.0.1 + fastapi/uvicorn/yfinance/feedparser, cargo 1.95.0 |
| P2 syntax + imports | ✓ 0 syntax error, 0 missing showme.* modules, 0 stale `from src.` |
| P2 JSON config | ✓ 4/4 (root + ui + tauri.conf + capabilities) |
| P2 tsc | ✓ clean |
| P2 eslint | ✓ 0 warning (CORR.tsx'te coverageRows/bugRows useMemo wrap) |
| P2 ruff | ✓ clean (engine/ exclude eklendi, kalan auto-fix yapıldı) |
| P3 backend pytest | ✓ **179 passed, 1 skipped** (21 deprecation warning, pre-existing) |
| P3 UI vitest | ✓ **21 dosya / 119 test passed** |
| P4 UI bundle | ✓ `ui/dist/`, max chunk vendor-charts 153kB / 50kB gz |
| P4 PyInstaller sidecar | ✓ 56MB binary, 19s boot (yfinance/pandas import maliyeti), `SIDECAR_PORT` ✓ |
| P4 Tauri full build | ✓ 2m49s release, `showMe.app` + `showMe_0.0.1_aarch64.dmg` (62MB) |
| P5 `/api/health` | ✓ `engine_attached: true`, `function_factory_warmed: true` |
| P5 `/api/sidecar/info` | ✓ Python 3.13.3, darwin |
| P5 `/api/function-index` | ✓ **141 fonksiyon** (≥138 baseline) |
| P5 `/api/fn/DES?symbol=AAPL` | ✓ provider_unavailable (downstream provider, expected) |
| P5 `/api/fn/CN?symbol=BTCUSDT` | ✓ Cointelegraph RSS canlı |
| P5 `/api/fn/BETA?symbol=AAPL` | ✓ SPY karşı 1Y β=1.0475, 2Y β=1.1539 |

## Yan etki: lint kapsamı

Refactor `engine/src/` ağacını ruff kapsamına dahil etti. Auto-fix sonrası
65 stil hatası kaldı (E701/E702 multi-statement, E741 `l` ambiguous, E731
lambda-assign). Bunlar **refactor tarafından oluşturulmadı**; engine'in eski
rejiminde gizliydi. Pre-refactor scope'u korumak için
`extend-exclude = ["showme/engine"]` eklendi. Sonraki round'da engine stil
temizliği ayrı bir cleanup olarak ele alınabilir.

## Snapshot

- Tag: `refactor-base-2026-05-09`
- Branch: `backup-pre-restructure`
- Pre-refactor base: `b9cdca6` (Add instant secondary news line)
- WIP snapshot commit: `e2e864c`
- Refactor commit: `6f79f5b` (629 dosya, +1860/−7893)
