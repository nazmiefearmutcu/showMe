# Round 31 — Preferences Streams diagnostics + lint/tooling cleanup

**Date:** 2026-05-01
**Goal:** Round 30'da açık kalan `Preferences > Streams` diagnostic
sayfasını mevcut `StreamHub.stats()` endpoint'ine bağlamak ve yerel
doğrulama komutlarını tekrar yeşile çekmek.

## Delivered

### Frontend — Preferences Streams

* `lib/sidecar.ts` → typed `fetchStreamStats()` client.
* `Preferences.tsx` → yeni `streams` sekmesi:
  channel count, subscriber count, live/idle/error status ve kanal
  tablosu (`symbol`, `subscribers`, `last_price`, `source`).
* Sekme 2.5 sn interval ile poll eder; manuel Refresh butonu vardır.
* EN/TR i18n kataloglarına `preferences.streams` eklendi.

### Frontend — ESLint 9

* `src-ui/eslint.config.js` eklendi; eski `.eslintrc.cjs`
  kuralları ESLint 9 flat config'e taşındı.
* TypeScript dosyalarında `no-undef` kapatıldı; TS compiler zaten
  DOM/React/RequestInit tiplerini doğruluyor.
* Fast-refresh uyarıları için CSV builder'lar komponent dosyalarından
  çıkarıldı:
  * `functions/HP.csv.ts`
  * `functions/TRAN.csv.ts`
* Hook dependency uyarıları:
  * `EQS` rows memoize edildi.
  * `PORT` positions memoize edildi.
* `Workspace` leaf tipi, runtime helper yerine `LeafNode` type import
  ile çözüldü.

### Python hygiene

* `ruff` import hataları temizlendi:
  kullanılmayan import'lar kaldırıldı, `test_scanner.py` import sırası
  düzeltildi.

## Verification

* `npm run lint` — clean.
* `npm --workspace src-ui run test` — **18 dosya, 91/91**.
* `npm run build:ui` — clean; bundle **462.92 KB / 138.85 KB gzip**.
* `uv run pytest -q` — **90/90**.
* `uv run ruff check .` — clean.
* Browser-mode smoke:
  * sidecar `http://127.0.0.1:8765/api/health` → ShowMe attached.
  * `http://127.0.0.1:8765/api/stream/stats` → `{"channels":[]}`.
  * Vite UI `http://127.0.0.1:5173/` → HTTP 200.

## Remaining open topics

* MOST sub-second top-N reorder (trader feedback gerekli).
* MOST/WCRS/GLCO opsiyonel stream subscriber'ları.
* PORT WHAT-IF + stress testing.
* IBKR / Coinbase / Binance live broker adapter'ları.
* Rust/Tauri compile smoke yerelde koşmadı: bu makinede `cargo`
  PATH'te değil.
