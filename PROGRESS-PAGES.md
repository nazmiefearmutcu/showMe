# ShowMe — Page-by-Page Development Progress

Ralph-driven, one page per iteration. Plan: `docs/superpowers/plans/2026-06-08-page-by-page-development.md`.

| # | Code | Page | Status | Notes |
|---|------|------|--------|-------|
| 1 | HOME | Overview | ✅ done | live movers (live:true), BRIEF wired + honest demo fallback, aria-labels + role=status, div role=row (a11y fix), tabular-nums, centralized formatters; 78 tests pass. commit 749a156 |
| 2 | WATCH | Live Watchlist | ✅ done | stable-node live pulse restart (no jitter+flash), Volume/Notional cols (real snapshot.volume, honest —), aria-labels + role=status/alert, terminal-grid-numeric, compact sparkline; all WATCH tests pass. commits 1f9c79d+de4f597 |
| 3 | PORT | Portfolio | ✅ done | broker positions surfaced in main view (frontend merge, real asset_class), crypto live quotes (backend), honest return badge (replaced fake sparkline), DRY formatters + terminal-grid-numeric, sorting + aria; UI 20 + backend 132 tests pass. commits 269becb+bc32a21 |
| 4 | WEI | Macro Monitor | ✅ done | world indices 11→31 (all regions, EM; single source of truth + baseline-drift guard), real as_of freshness, "Model data" badge + honest de-emphasized synthetic sparklines, fixed false Degraded banner, format.ts+terminal-grid-numeric, sorting + a11y + responsive KPI; UI 9 + backend 87 tests pass. commit ee1e2ca |
| 5 | NI | News Desk | ✅ done | honest states (provider-unavailable → error banner not fake cards; removed ~290 lines of fake "pipeline log" animation → honest loader); headlines unblocked from async Veryfinder; keyboard-nav + list semantics + aria-labels + line-clamp headlines; data is real (RSS/GDELT/FinBERT/Veryfinder). UI 10 tests pass. commits 3d3f4c7+24e905b |
| 6 | SCAN | All Functions | ✅ done | NOTE: SCAN is a TRADING SCANNER (not a function catalog). Removed synthetic per-row "Trend" sparkline (honesty); fixed dbl-click double-fire + Reset&retry stale-closure + Long/Short color collision; keyboard a11y (focus-visible/cursor, Enter→DES launch, aria-labels), empty/error Retry, mono/CSS-class polish; scanner data confirmed REAL (no stub). NEW SCAN.test.tsx 11 tests. commits 227cfec+936207c |
| 7 | MIS | Multi Indicator Scan | ✅ done | data verified REAL (23 indicators on real multi-TF OHLCV, no stub); added indicator drill-down (inline expandable, useSyncExternalStore — memo-stable cols), a11y (aria-labels, TF fieldset, confidence role=meter, inline no-markets alert + select-all, progress-on-click), DRY formatters + terminal-grid-numeric, skip-reason tooltips. UI 12 + 356 src/functions tests pass. commits d835528+9090272 |
| 8 | PREF | Settings | ✅ done | settings verified persist+apply (theme/density/locale/tz/sidebar/secrets REAL); a11y radiogroups+aria-checked (density/lang/tz/migration-mode), icon-button aria-labels, async role=alert/status/aria-busy (assertive on errors); bug fixes: reset→home preset (papyrus), hex inline error+restore-on-blur; NEW migration path/mode persistence (lib/migration-prefs.ts); focus-rings + themed PreferencesChrome. UI 128 prefs+i18n tests pass. commits 33ce821+(review fixes) |
| — | — | **Wave 1 complete (8/8)** | ✅ | HOME·WATCH·PORT·WEI·NI·SCAN·MIS·PREF |
| 9 | GEX | Gamma Exposure | ✅ done | DATA HONESTY: synthetic 3-strike fallback now explicitly flagged (backend summary.synthetic/degraded + source_mode + warning) + prominent "⚠ Synthetic model" badge + role=alert banner (live path unflagged); "nearest expiry only" disclosure; DRY formatters + terminal-grid-numeric + strike-jitter fix; flip-point marker + wall emphasis + axis contrast; row aria-labels; NEW GEX.test.tsx (11) + backend honesty test (5). UI 11 + backend 12 + full 1203 pass. commits ea95739+(review fixes) |
| 10 | FA | Financial Analysis | ✅ done | data verified REAL (SEC EDGAR XBRL → yfinance fallback → honest provider_unavailable empty); removed DEAD synthetic _financial_snapshot_model; real filing_date provenance (tz-safe); DRY formatters (dropped formatCell/formatHeroValue bespoke), negatives red+parens, humanized headers, terminal-grid-numeric + tone classes; ariaLabel (single) + retry a11y; NEW FA.test.tsx (13) + backend filing_date tests (3). commits ecf1f48+8580b0c |
| 11 | DES | Description | ✅ done | data verified REAL (yfinance .info/CoinGecko per-symbol, honest empty); DRY formatters→format.ts, sign-colored+currency abs-change, live price flash, summary clamp/expand; link aria-labels+focus; backend crypto-ID mismatch GUARD (wrong-coin→honest provider_unavailable; base-symbol separator fix); format.ts trailing-zeros fix (non-compact). NEW DES.terminal-grade.test.tsx (8) + backend guard tests (4); full UI 1227 + backend 133 pass. commits 6891e70+b36ee19 |
| 12 | BTMM | Rates Environment | ✅ done | NOTE: BTMM = central-bank POLICY-RATE monitor (BIS CBPOL ~45 banks), NOT a yield curve. data REAL (live BIS); HONESTY: synthetic sparkline→muted placeholder, prominent fallback-snapshot banner, freshness-only stale pill (not "live" on stale cache); fixed bps color convention (consistently inverted: hike=red/cut=green), neutral at 0bp; SVG role=img, search a11y, KPI tooltips; DRY formatters + terminal-grid-numeric. NEW BTMM.terminal-grade.test.tsx; 15 BTMM tests pass. commits e974564+109072c |
| 13 | MOST | Most Active | ✅ done | data honest (live_screen:true→real yfinance, honest empty on fail, labeled REFERENCE only when live=false); fixed stack-overflow (maxOf), EXCISED synthetic deterministicTrend (per-row col + KPI sparklines), backend row-level quote_state, aria-labels, DRY formatters + terminal-grid-numeric. NEW MOST.test.tsx (13) + backend (3). commits fa41ffc+(honesty fix) |
| 14 | INSTANT | Squawk Line | ✅ done | NOTE: INSTANT = real-time squawk/event FEED (not a trade ticket); data REAL+honest (instant microservice RSS/GDELT/X + SQLite fallback + transport mode; real source health; no fake). a11y: SR-safe aria-live (separate sr-only announcer, not full-list spam), chip aria-pressed, backfill aria-busy; bug fixes: stable+unique event keys, new-only flash (seen-set, no mass-flash); honest hardcoded-speedup caption (live vs documented vs connected-empty); English relative time; terminal-grid-numeric. NEW INSTANT.terminal-grade.test.tsx; full UI 1259 pass. commits d98a604+a9c84eb |
| 15 | ALRT | Alerts | ✅ done | MARQUEE: alerts were a non-functional CRUD shell (never evaluated) — implemented REAL client-side eval loop (45s visibility-gated poll, honest edge-trigger for above/below + actual-crossing for cross_*, 5min cooldown, prev-value tracked before cooldown so crossings aren't lost); live Current value + armed/triggered status; honest "client-side/foreground-only" disclosure caption; a11y (role=switch, aria-labels, form onSubmit, aria-describedby); display (formatPrice tabular, --negative/--font-mono, Empty CTA). NEW ALRT.eval.test.tsx (8: edge/cross/cooldown/first-obs/no-quote). review SPEC PASS + QUALITY APPROVE. commits 7e5d9a1+(test+guard) |
| 16 | STRA | Strategy Editor | ✅ done | data largely REAL (disk persistence, Pydantic validation, real indicator catalog shared w/ MIS, real rule-eval engine); HONESTY: disclosed preview runs on SYNTHETIC random-walk data (gated on real `source` field); fixed 4 dead CSS tokens (--accent-err/--fg-2/--border-1/--accent-warn → real tokens; list separator now visible); a11y (aria-labels on indicator/operator selects + remove/delete, Save disabled-reason title); rule-row grid alignment + entry/exit grouping panels + tabular threshold; Empty/Skeleton states. NEW STRA.terminal-grade.test.tsx (11); 47 STRA tests pass. (Note: backtest-metrics endpoint + param-tuning + real-OHLCV preview are larger future work, out of page scope.) commits c1e3816+(token fix) |
| 17 | TMPL | Strategy Templates | ✅ done | data REAL (12 valid templates → real instantiate→persist→edit round-trip); ROOT-FIX of cross-cutting dead CSS tokens: aliased --border-1/--fg-2/--accent-err/--accent-warn/--accent-ok in tokens.css (fixes shared ConfirmDialog/AppErrorBoundary + 9 panes for free) + canonical in TMPL.tsx; backend instantiate now validate_against_catalog (400 on bad indicator, mirrors strategies route); a11y (aria-live/role=alert status, modal aria-labelledby, aria-current, label/id, focus-trap), loading/Empty states. NEW TMPL.terminal-grade.test.tsx (9) + 2 backend tests; review SPEC PASS+APPROVE. commit 6b73308 |
| 18 | BOT | Bot Manager | ✅ done | DATA HONESTY: live risk_pct orders now resolve broker equity ONCE per tick and tag SignalEntry.equity_source ("broker"/"fallback_10k") so a $10k-fallback-sized order is flagged with a warn "≈$10k" badge in the signal log (prev. silent); perf routes (leaderboard + per-bot) now use uncapped closed_trades_log not the 100-cap signal_log → correct PnL for long-running bots (also de-risks PERF Task 20). a11y: StatusPill→design-system Pill (live=neg/shadow=warn/off=muted)+role=status, signal table caption+scope=col+aria-label+terminal-grid-numeric, form htmlFor/id + aria-describedby error links, pane error role=status/aria-live now visible even with no draft. usability: ConfirmDialog before Disable, live re-type confirm only during shadow→live transition, disabled-Save title reason. display: formatPrice (adaptive sub-cent) replaces .toFixed(2), stable signal-row keys. NEW BOT.terminal-grade.test.tsx (19) + backend (perf-closed-trades + equity-source + single-resolution counter regression). review SPEC PASS; QUALITY REQUEST_CHANGES→all fixed (P1 double-account()/honesty, F4 error visibility, key stability). full UI 1304 pass / backend 36 pass. commits 3099d09+6fd65c6+2c7e153+2002076 |
| 19 | BOTS | Bot Supervision | ✅ done | DATA SUFFICIENCY/HONESTY (core supervision gap): page showed `enabled` AS health — a crashed/stuck bot still read LIVE/SHADOW. Backend `/api/bots` now exposes (network-free, from already-loaded rec) `is_running` (cheap runner task-done check), `last_event_at`, `last_action`; UI derives a REAL health taxonomy: OFF(!enabled) · STUCK(enabled&&!is_running) · DEGRADED(running&&last_action="skipped") · LIVE/SHADOW(healthy). `is_running` returns null (not False) on runner-introspection error → UI legacy fallback, no false-STUCK flood on restart. equity_source="fallback_10k" surfaced as warn "≈$10k" badge in feed (honesty, matches BOT). usability: per-bot "Son tick Nm ago" freshness + stale de-emphasis, KPI Stuck/Degraded count, refresh aria-busy/disabled, feed-window disclosure. a11y: ModePill→design-system Pill+aria-label, SINGLE pane-level sr-only role=status summary (no per-row SR-spam on 10s poll), table caption+scope=col+terminal-grid-numeric, error role=status reachable w/o bots. display: formatPrice, Empty/Skeleton states, stable feed keys (no index shift). NEW BOTS.terminal-grade.test.tsx (~22) + backend (health fields + is_running-null-on-error). review SPEC PASS; QUALITY REQUEST_CHANGES→fixed (P2-A SR-spam, P2-B false-STUCK, key/aria cleanup). full UI 1328 pass / backend 20 pass. commits ac44ca9+3d012fc+748cfe9+bb762f4 |
| 20 | PERF | Performance | ✅ done | DATA HONESTY/SUFFICIENCY: equity curve is a SIMULATED $10k-relative curve (not real balance) — now explicitly disclosed via returned `starting_equity` + disclaimer next to the chart; backend detail route exposes `equity_source` (most-recent live signal) → warn "⚠ yedek $10k equity" when fallback_10k; both perf routes return `generated_at` (freshness → "son güncelleme"). Surfaced backend-computed-but-HIDDEN risk metrics (sharpe/sortino/profit_factor/expectancy/max_consec_losses) in detail WITH sample-size N + low-N(<20) caveat/de-emphasis; profit_factor "inf"→"∞" handled (no NaN). DISPLAY: drawdown now reads as a LOSS (negative/red) in leaderboard+detail; all .toFixed→format.ts helpers (added formatNumber minimumFractionDigits opt for fixed-decimal ratios); terminal-grid-numeric on both tables. A11Y: tables caption+scope=col+aria-label; equity SVG role=img+aria-label+sr-only summary; refresh aria-busy/disabled; keyboard-operable leaderboard rows+aria-selected; single sr-only role=status summary (no poll SR-spam); KPI pill names; close-btn label. STATES: Skeleton/Empty (leaderboard+empty curve), last-updated. NEW PERF.terminal-grade.test.tsx (26) + backend perf-route (generated_at/starting_equity/equity_source incl most-recent-wins). review SPEC PASS; QUALITY REQUEST_CHANGES→fixed (dead summary ref, ratio via format.ts, test gap). full UI 1358 pass / backend perf 21 pass. commits 078fc6c+7000813+18dacc3 |
| 21 | BDA | Bot Dev Assistant | ✅ done | HONESTY-LED (assistant overclaim was the core issue): parser is keyword/regex (NOT an LLM) but UI branded "NL → Strateji" implying semantic understanding + silently dropped unsupported sentence parts. FIXES: parser now ECHOES ignored concepts ("⚠ 'divergence' desteklenmiyor — yok sayıldı"; same for stop-loss/TP, risk/sizing, trailing, candlestick/pattern, and "only first indicator used" on multi-indicator input) via 2-tier match (short tokens sl/tp/risk/mum as WHOLE tokens, not substrings — regression-tested both directions); discloses defaults honestly (timeframe "1h (varsayılan — belirtilmedi)" vs parsed; injected position/stop-loss defaults noted); fixed EMA "20 50" bug (was ignoring 50 + hardcoding period*3 → now uses both). explain() found to be TEMPLATED/deterministic (not AI) → UI caption says so. Route now validate_against_catalog (skips gracefully when catalog unavailable — not reject-all) → never persists catalog-invalid spec. UI: de-overclaim help caption listing the REAL supported indicators ("gerçek NLP değil"); notes severity-color-coded (warn/ignored prominent, not gray); a11y (textarea label htmlFor/id+aria-describedby, role=status error, labelled live result region, aria-busy buttons, explain select label, monospace explanation); states (Skeleton/Empty, clear-after-save, Cmd+Enter); display (Pill saved-id, accent-on token, mono). NEW BDA.terminal-grade.test.tsx (15) + backend parser/route (ignored-concepts incl negative false-positive guard, EMA dual, catalog skip/invalid). review SPEC PASS; QUALITY APPROVE w/ fixes (empty-catalog reject-all bug, false-positive guard tests, redundant EN aria-label). full UI 1371 pass / backend assistant 35 pass. commits 9441dfd+74711da+2afbb29+b430f99 |
| 22 | INDX | Indicator Index | ✅ done | catalog data verified REAL+substantive (15 indicators: real formulas, params w/ ranges, Wilder/Appel/Bollinger refs, suggested strategies). HONESTY: `confidence` (1-10) is a SUBJECTIVE editorial score shown as if authoritative → added visible disclosure ("Güven = öznel editör değerlendirmesi, backtest değil") + title tooltip + honest rationale heading ("Değerlendirme gerekçesi (öznel)") + suggested_strategy labeled "illüstratif — doğrulanmamış"; YAML maintainer note (data/schema unchanged, 67 catalog tests green). DISPLAY BUG: `--fg-1` was UNDEFINED in tokens.css → fixed to --text-primary (+ migrated --fg-2→--text-secondary in file). a11y confidence now role=meter+aria-valuenow/min/max (color no longer sole signal). STATES (store had loading/error, pane IGNORED them): Skeleton loading + role=alert error + design-system Empty (distinct copy for genuinely-empty vs filtered). a11y: search label htmlFor/id, family buttons aria-label+role=group, param table caption+scope=col, detail pane role=region (tested), result-count role=status. display: Pill family tags, monospace formula/params. NEW INDX.terminal-grade.test.tsx (14) incl --fg-1 source guard. review SPEC PASS; QUALITY APPROVE→nits fixed (error role=alert, A4 test, empty-catalog copy). full UI 1385 pass / backend catalog 67 pass. commits 685cb99+b2f6229+9d2ace3 |
| 23 | CONN | Connect Exchange | ✅ done | SENSITIVE (API keys). SECURITY AUDIT first: secret core already SOLID (A-) — secrets in OS keychain/memvault, metadata-only JSON index, logs `_scrub()`'d, responses never return api_key/secret (explicit redaction tests), connection "test" is a REAL broker.account() call. So kept secure core UNTOUCHED, improved the other dims. DATA/HONESTY: added `last_verified` (metadata-only, stamped ONLY on successful /test, survives reload, never overwritten by a later failure); status is now HONEST — green "Doğrulandı" ONLY for an in-session successful test, prior-session verification shows muted "Daha önce doğrulandı" (not green-as-if-live), failed="Başarısız", none="Denenmedi". UX/a11y: secret fields get show/hide toggle (local-only, default hidden, aria-pressed); native blocking confirmAction()→ in-app design-system ConfirmDialog for delete (focus-trap/Esc) with EXACT force/cascade/bots-unknown("doğrulanamadı") logic preserved + single dependents fetch (was double, could diverge); form label htmlFor/id (incl upgrade re-confirm, was placeholder-only), role=status error + aria-describedby, client-side required + disabled-reason; aria-busy/disabled/title on Test/Upgrade/Bağlan; test-result role=status + aria-describedby; permission→Pill, Empty/Skeleton states, filter aria-labels, --fg-1→--text-primary. NEW CONN.terminal-grade.test.tsx (~18) + backend (last_verified stamp/persist/failure-preserves/metadata-safe). review SPEC PASS + security clean; QUALITY REQUEST_CHANGES→fixed (double-fetch race, stale-green honesty, aria-describedby, test gap). full UI 1407 pass / backend exchange+redaction 29 pass. commits c002ef0+7181869+cf63104+049db35 |
| 24 | TXNS | Trade Blotter | ✅ done | DATA HONESTY (rows are REAL but their NATURE was unclear): blotter shows trade history IMPORTED into local portfolio.db (from runtime/state.json) — NOT live broker fills, and NOT ShowMe bot trades (bot closed_trades aren't persisted to this DB). Added prominent provenance disclosure (shows the source db path) + surfaced the real per-row `source` column ("Kaynak", + in CSV); clarified that `mode` ("read_only"/"writable") is a DB-EDIT-PERMISSION flag NOT live-vs-shadow trading (header "Kayıt" + pill title/aria "writable = DB'de düzenlenebilir kayıt; canlı işlem anlamına gelmez"). Backend: /api/state/trades now returns `generated_at` → "Son güncelleme: HH:MM UTC" freshness (honestly labeled response-time, not import-time). DISPLAY: bespoke fmtNum/fmtDate→format.ts (formatNumber/formatPrice/formatMissing), terminal-grid-numeric numeric cells. A11Y/UX: DataGrid ariaLabel + sortable columns (closed_at default desc, aria-sort), symbol-button aria-label, error role=status (fixed latent bug: error now checked BEFORE skeleton so a fetch reject doesn't spin forever; pill/refresh show error+retry not loading), CSV button aria-label, result count "N/M", empty-state distinguishes empty-db vs filtered-to-zero. SCOPE: deferred (out of one-page scope, noted as followups) bot-trades→DB persistence, fee column+migration, live broker-fill reconciliation, shadow/live ClosedTrade mode. NEW TXNS.terminal-grade.test.tsx (12) + backend generated_at test; (recovered from a mid-run API error — backend/CSV parts were already applied, completed frontend on top). review SPEC PASS; QUALITY APPROVE→fixed (error-masked-as-loading, UTC clock label). full UI 1419 pass / backend state 42 pass. commits 6ed14bd+1f09162+47b34b7 |
| 25 | AGENT | Symbol Agent | ✅ done | HONESTY (de-overclaim — recurring "Agent implies AI" pattern): backend ranker is a DETERMINISTIC heuristic (NOT an LLM); default mode scores via TRANSPARENT SYNTHETIC "fast probe" payloads (not live per-symbol exec). Backend response was already honest (returns method/methodology/probe_mode) but UI never surfaced it → added visible disclosure (`agent-methodology`: "deterministik sezgisel sıralayıcı… yapay zekâ/LLM değil… şeffaf sentetik probe… gerçek yürütme için Canlı mod") + surfaces real result.method/methodology after run; relabeled "confidence"→"sinyal yoğ." (signal density from keyword-match, NOT model uncertainty, w/ tooltip); exposed the backend's `execute_functions` LIVE toggle (default OFF=fast probe, clearly labeled) so synthetic-default is honest by contrast + adds real exec lever. BUG: header Run button was UNSTYLED (btn classes wrongly on wrapper div) → fixed (btn--accent on button + aria-busy). DISPLAY: score + signal-density as role=meter (aria-valuenow clamped to band — review-caught WCAG fix), monospace symbols, DataGrid ariaLabel, Skeleton, styled error role=status. USABILITY: ranked symbol rows now actionable (button→navigate DES, SCAN pattern, keyboard), result-count role=status. Code-quality: stable jumpToDES useCallback memo, honest pre-run method placeholder. NEW AGENT.test.tsx (17, was only lib parseCandidateText test) incl out-of-band aria clamp + execute_functions threading + navigate spy. review SPEC PASS; QUALITY REQUEST_CHANGES→fixed (aria-valuenow clamp, stale closure, pre-run method). full UI 1435 pass; backend untouched (already honest). commits 2f5afbe+5d3feb6 |
| 26 | ASK | Ask | ✅ done | HONESTY (the heaviest de-overclaim yet — biggest pane 29.5KB): the ANSWER (narrative/highlights/viz) is produced by a DETERMINISTIC rule-based summarizer (no LLM); only the PLANNER conditionally calls an LLM (haiku/gpt-4o-mini, NOT Sonnet). UI was LYING: hardcoded model "claude-sonnet-4.6" + FAKE cost (0.005+elapsed*0.00005) + "Ask Agent"/thinking framing implying AI-written answers. FIX: backend `/api/ask` now returns REAL provenance (`plan_method`/`model_used`/`provider`/`cost_usd`/`was_llm_called`) derived from the actual planner `CostEntry` (review-hardened: names the provider that ACTUALLY ran on fallback, not providers[0]; no ledger-delta race; crash-safe). UI shows real model (or honest "kural-tabanlı plan") + real cost ($0.00 when deterministic, no fake min) + per-turn plan-method badge ("Plan: AI (model)" vs "kural-tabanlı") + disclosure that the answer is deterministically composed from real function outputs, not AI-written; de-overclaimed "Ask Agent"→"Ask". BUG: undefined `@keyframes showme-ask-pulse` (thinking dots didn't animate) → defined. A11Y: textarea label htmlFor/id, submit aria-busy/disabled-reason, each answer+error turn role=status/aria-live (stable keys → no SR re-announce), citations/evidence now ACTIONABLE buttons (Enter/Space→open cited fn via navigate)+aria-label. USABILITY: suggestion chips disabled while running, Esc + Stop button cancel in-flight (abortable fetch). citations→Pill, formatCurrency. citations are genuinely function-backed (real execution/row counts) though narrative↔evidence link is positional (followup). NEW ASK.test.tsx (18) + backend provenance/CostEntry/multi-provider-fallback tests. review SPEC PASS; QUALITY REQUEST_CHANGES→fixed (multi-provider model mislabel honesty bug, ledger race, test mocks, Esc test). full UI 1453 pass / backend 47 pass. commits 5710bd3+1a90a6f+f22ab54+40a06c0 |
| 27 | XSEN | X Sentiment AI | ✅ done | DATA VERIFIED REAL (rare — AI claim is ACCURATE here): posts genuinely scraped from X via public backends (Brave/DDG/Bing/Nitter/Jina + Twitter syndication CDN, real HTTP, no mock); sentiment scored by a REAL locally-run fine-tuned RoBERTa model (showme_x_v1, 3 task heads) — not LLM/heuristic. So the fix REINFORCED honesty, didn't de-overclaim. ONE real gap = FRESHNESS: search tweet-ID lists cached ~30min but UI showed `scrape_seconds` (hydration duration) as if freshness; no as-of; per-tweet `date` unused. FIX: backend adds `fetched_at` (served-time) to analyze response; UI shows "Veri alındı: Nm önce" + RELABELS scrape_seconds→"analiz süresi" (w/ title: not data age) + ~30min cache disclosure (engagement refreshes each run) + per-tweet "Nm ago" from examples[].date with honest "tarih yok" (NEVER fabricated now) + scoring disclosure naming the local RoBERTa model ("AI rationale"→"RoBERTa sınıflandırması"). A11Y: BullishGauge role=meter+aria-valuenow(clamped -1..1), Run aria-busy/label, input htmlFor/id, tweet open-link/dot/toggle aria-labels, distribution cards aria-label breakdown, ONE SR-safe gated live region (load announce), Since chips aria-pressed. DISPLAY: confidence/engagement formatNumber+monospace, .toFixed%→formatPercent, bullish→formatSignedDelta. NEW XSEN.terminal-grade.test.tsx (~13, frozen-clock relative-time) + backend fetched_at test. SCOPE: deferred mass inline-style/Field refactor (~40 objs) + exact cache-age threading + INSTANT-events `date or now` fabrication (followups). review SPEC PASS; QUALITY APPROVE→fixed (test clock-freeze, Since aria-pressed). full UI 1465 pass / backend x 28 pass. commits ea51866+e0b6e25+88e0fd6 |
| 28 | CORR | Correlation | ✅ done | (biggest pane 48KB) MATH verified CORRECT (backend untouched): pandas Pearson/Spearman on aligned log/simple returns, diagonal 1.0, symmetric. HONESTY (two things already in payload but HIDDEN): (1) synthetic-fallback — when a symbol can't be fetched live the backend silently substitutes a DETERMINISTIC reference series (even with baked-in cross-market corr) and only a "6/7 live" count was shown → added PROMINENT warn banner above heatmap (`corr-synthetic-warning`) NAMING the synthetic symbols + `~`/title markers on their matrix row/col headers + "(sentetik leg)" in cell aria-labels; coverage-table pill now warn for BOTH computed_fallback AND computed_reference (shared `isSyntheticStatus` predicate, DRY). (2) per-pair sample size n was hidden (n=10 looked like n=100) → cell aria-label/title now "{a}–{b} {metric}: {v}, n={obs}" + frontend low-n(<20) warning (`corr-low-n-warning`). A11Y: matrix `<caption>`+scope=col/row, Run aria-busy+double-submit guard, error role=status, universe textarea htmlFor/id. DISPLAY: local formatCorr/Num/Percent → format.ts so missing sentinel is app-wide "—" not "N/A". FRONTEND-ONLY (data already present), no backend change. NEW CORR.test.tsx (12, was none). SCOPE: deferred mass inline-style migration (~600 LOC), chip symbol-input, matrix keyboard arrow-nav, backend forbid-synthetic-in-live option, faint-cell WCAG contrast. review SPEC PASS; QUALITY APPROVE→fixed (reference-pill honesty inconsistency + 2 test gaps). full UI 1477 pass. commits af78f06+c05b7ed |
| 29 | MAP | Market Heatmap (SECT alias) | ✅ done | data REAL (25 country ETFs / 11 SPDR sector ETFs, real yfinance live quotes; model-mode synthetic but disclosed). HONESTY (backend already exposed the truth, UI hid it — FRONTEND-ONLY): (1) tile SIZE is |% change| magnitude NOT market cap (treemap implies importance) → honest "Kutu boyutu = % değişim büyüklüğü (piyasa değeri DEĞİL)" note; (2) SECT live-mode delivers only 1D even when MTD/QTD/YTD selected → reads backend `change_pct_period` + `warnings`, labels ALL surfaces (tiles/movers/column/stat) with DELIVERED period, fires `map-period-notice` role=status on real mismatch; (3) model/fallback rows rendered like live → prominent `map-model-badge` role=status (metadata.degraded/fallback or all quote_type=model). A11Y: tiles→focusable buttons (navigate DES) + aria-label "{name}({etf}) {±%} {period}[ model]", legend movers→buttons, loading aria-busy, error role=status, SOURCE/WINDOW aria-pressed. DISPLAY: ratio-driven tile-text contrast (HeatCell-style, was same-tone-on-tone low-contrast), formatPercent(signed) vs manual toFixed, fetched_at "as of". NEW MarketHeatmap.terminal-grade.test.tsx (12). SCOPE: deferred design-system consolidation, real market-cap sizing (backend), as_of emission, responsive grid; map_seed.py manifest still claims cap-weighting → backend/docs reconcile followup. review SPEC PASS; QUALITY REQUEST_CHANGES→fixed (spurious "ok" status notice on clean payloads + negative test, legend border reset). full UI 1489 pass. commits 0ed880b+8ebab32 |
| 30 | TOP | Top News (plan mislabeled "Top Movers") | ✅ done | PLAN-REALITY MISMATCH: TOP is a ranked-NEWS pane (not gainers/losers) — real RSS/GDELT headlines + REAL deterministic importance_score ranking (disclosed importance_reasons) + FinBERT sentiment; data/ranking/freshness genuinely real (NOT de-overclaimed). HONESTY gap = the per-headline Veryfinder SOCIAL overlay runs in fixture/fallback (demo) mode when no X bearer token but UI showed "VF +45 BULLISH 82%" as real → now discloses [DEMO]/[YEDEK] marker (`top-vf-fixture`) + title reason (reads overlay.fixture_mode/fallback_mode/source_fallback_from); dedicated Veryfinder StatCard shows DEMO (was confusingly on Sources card). Also dropped SYNTHETIC `deterministicTrend` KPI sparklines (campaign anti-pattern, like HOME/MOST/WEI). A11Y: headlines→`<ul role=list>` with the TITLE as the primary activator button (Enter/Space opens source) + symbol/source as SIBLING controls (review fix: de-nested — no interactive-in-interactive ARIA violation), single shared gated VF live region (was per-card aria-live SR-spam), source/symbol aria-labels, sentiment by TEXT not color. DISPLAY: title/summary line-clamp + title attr. honest sort label "ÖNEM → YENİ" (was misleading "RECENT FIRST"; sorts importance-then-recency) + IMPACT composite tooltip. perf: single VF fetch per article-set (removed `state` from effect deps). FRONTEND-ONLY. NEW TOP.test.tsx (13) + S04R regression updated. review SPEC PASS; QUALITY REQUEST_CHANGES→fixed (nested-interactive a11y, double VF fetch, synthetic sparklines, DEMO placement). NOTE: NI.tsx carries the SAME nested-interactive pattern → cross-cutting followup. full UI 1501 pass. commits 4617137+3d4ad0d |
| 31 | ECO | Economic Calendar | ✅ done | data REAL via TradingEconomics+Finnhub providers; honest value-handling (missing actual→surprise None→"—"). HONESTY gap: when BOTH providers fail backend SILENTLY fell back to `_calendar_feed_model()` (hardcoded synthetic calendar with FABRICATED past actuals) shown only as raw "calendar_feed_model" source string → now (1) backend appends honest warning on fallback + NULLS the fabricated synthetic actuals at source (schedule shows, no fake prints) + adds real `as_of`; (2) UI prominent "ÖRNEK TAKVİM — canlı veri yok" badge (`eco-model-badge` role=status, shown in BOTH populated AND empty states — review fix) + footer "calendar_feed_model"→"Örnek takvim (canlı değil)". freshness from real as_of (not client clock) + "Zamanlar UTC". A11Y: importance non-color cue (▲▲▲/▲▲/▲ + word + aria-label "önem: ..."), DataGrid ariaLabel, error role=status, filter aria-labels, refresh aria-busy (additive `busy?` prop on shared RefreshButton — opt-in, no regression). DISPLAY: local fmtNum→format.ts (formatMissing "—"), surprise sign-colored DeltaChip. shared design-system tweaks (Pill aria-label passthrough, Tabs ariaLabel) additive/backward-compat. NEW ECO.test.tsx (10) + backend test_eco.py (3). SCOPE: deferred inline-style migration, day-grouping, sortable, auto-poll; manifest FRED/RSS-vs-TE/Finnhub spec drift = followup. review SPEC PASS; QUALITY APPROVE→fixed (empty-state badge, refresh aria-busy opt-in after catching a MarketHeatmap regression). full UI 1511 pass / backend eco 29 pass. commits d645cf3+e70ab1a+9a5fdc3 |
| 32 | PORTX | Portfolio Analytics (20 codes) | ✅ done | ONE shared pane serving 20 codes. Backend MATH verified correct (PORT_OPT/PVAR/RPAR/STRS/BLAK/backtests sound) + holdings REAL (portfolio.json). HONESTY (the big shared surface): many codes silently fall back to DETERMINISTIC SYNTHETIC templates when live data unavailable (PORT_OPT/TRA/MARS `*_model` sources, RPAR `_fast_template`, TLH/MGN sample, PVAR `data_mode:modeled`, BLAK `return_data_state:synthetic_fallback`, MLSIG/BTUNE/BTFW `status:reference`) with INCONSISTENT per-code disclosure the shared pane never surfaced → added ONE shared `portfolioDataMode()` classifier (live|modeled|sample|degraded) reading the heterogeneous backend signals (status/data_mode/source_mode/return_data_state/fallback/metadata + sources regex /model|template|synthetic|sample/i — "reference" only on controlled enum fields to avoid false-MODEL on a live "*_reference_*" provider) + prominent `portx-data-badge` (role=status) in BOTH populated & empty branches; fires for synthetic, silent for live. Lifts all 20 codes at once, FRONTEND-ONLY (no 20-fn backend rewrite). DISPLAY: sign-color financials (.u-text-negative/positive) ONLY for sign-meaningful keys (pnl/return/alpha/delta/drawdown/...) across KPI cards+table cells+ladder; fixed ladder value misformat (was hardcoded weight_pct→row.labelKey, total_pnl showed "120,000%"); empty-actions class fix. A11Y: per-code input labels htmlFor/id, DataGrid ariaLabel, RefreshButton busy, warnings+error role=status (de-duped). USABILITY: added 8 codes missing from toolbar (ACCT/BMTX/BTFW/BTUNE/LOTS/MGN/MLSIG/TLH) — all reachable. NEW PortfolioAnalytics.test.tsx (20). SCOPE: deferred backend source_mode normalization across 20 fns, --fg-1 in AppErrorBoundary, per-code obs-count warnings, frontier max-Sharpe highlight, freshness badge. review SPEC PASS; QUALITY REQUEST_CHANGES→fixed (ladder format key, synthetic-regex false-positive, dup live region). full UI 1531 pass. commits c8fd45c+749601f |
| 33 | AIM | Cross-broker order ledger | ✅ done | AIM = read-only cross-broker order blotter (live open orders across binance/alpaca/ibkr/oanda + persisted order_history tail; symbol-scoped; 15s poll; data_mode live_exchange/cached_snapshot/not_configured). Data genuinely REAL with honest fallback (no synthetic orders) — the gaps were 2 MISLABELS + a11y + display, NOT fabrication. HONESTY: (H1) "Filled Today" KPI actually counts ALL-TIME filled/partial rows in the history tail, not today's → renamed to "Filled" in BOTH manifest CardSlot label (aim_seed.py) AND frontend StatCard (data key `filled_today` kept for contract stability, honestly labeled); (H2) manifest ProviderChain `primary="ccxt_broker"` was wrong (impl fans out across real broker adapters) → `primary="broker_adapters"` (cached_snapshot fallback preserved; no validator/allowlist rejects it); (H3) data_mode degraded/cached now EXPLAINS itself via dataModeTitle/dataModeExplanation notice (`aim-mode-notice` role=status, not color-only) + freshness "AS OF {as_of} UTC" from REAL server as_of (extractAsOfHHMM; honest client-clock fallback only when absent). A11Y: (A1) live region SCOPED to loading/error transitions only — initial outer role=status wrapped the whole PaneBody so the grid re-announced every 15s poll; review P2 → re-scoped (loading Skeleton + error Empty each get role=status/aria-live; steady-state grid sits OUTSIDE any live region; also resolves nested-live-region conflict with the mode notice); (A2) RefreshButton busy; (A3) SideChip→Pill + StatusPill aria-labels (yön/durum, not color-only); (A4) KPI ribbon aria-label + data_mode pill aria-label. DISPLAY (D1): SideChip span→design-system Pill; fmtNum→formatNumber(v,6), fmtNotional→formatCurrency(compact), missing→formatMissing; fmtTime kept w/ comment. provider_errors role=alert vs warnings role=status. FRONTEND + manifest-label + manifest-provider only (_funcs.py untouched — rename is label-only honesty). NEW AIM.test.tsx (15, +1 regression: grid not inside [role=status]) + backend test_degarbage_trade_funcs.py (+2: honest provider chain, Filled-not-today label). review SPEC PASS (H1-3,A1-4,D1 met); QUALITY: 1 P2 (live-region SR-spam on poll)→FIXED + re-verified, P3s (qty precision, var-name comment) deferred as nits. full UI 1546 pass / backend trade 71 pass. commits 6416086+a69a764+df7db1b |
| 34 | ANR | (discover) | pending | |
